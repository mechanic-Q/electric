---
description: Generate structured specification — proposal + design + task breakdown
argument-hint: "[需求] [--change <name>]"
tools:
  read: true
  write: true
  edit: true
  bash: true
  glob: true
  grep: true
  agent: true
  question: true
---
Run `sillyspec run propose $ARGUMENTS` to produce structured spec output.

Three deliverables: proposal doc, design doc, task breakdown.
