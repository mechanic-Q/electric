# Showcase WebUI minimal runtime
# Only what the public server needs: FastAPI + data loading + DeepSeek chat
# NO xgboost, shap, scikit-learn, gymnasium, stable-baselines3, tensorboard, jupyter, plotly
# Install: pip install -r requirements-showcase.txt

# ── API server ──────────────────────────────────────────
fastapi>=0.136.1
uvicorn>=0.47.0
pydantic>=2.13.4
typer>=0.15

# ── Data loading (reports, datasets, capabilities) ─────
pandas>=2.0.0
numpy>=2.0.0
pyarrow>=15.0.0

# ── LLM agent (DeepSeek chat via LangChain) ────────────
langchain>=1.3.1
langchain-openai>=1.2.2
httpx>=0.27
