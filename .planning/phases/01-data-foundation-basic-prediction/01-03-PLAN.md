---
phase: 01-data-foundation-basic-prediction
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - docker-compose.yml
  - README.md
  - docs/chinese-electricity-data-guide.md
  - notebooks/01_data_ingestion.ipynb
  - notebooks/02_data_cleaning.ipynb
  - notebooks/03_feature_engineering.ipynb
  - notebooks/04_load_forecasting.ipynb
  - notebooks/05_end_to_end_baseline.ipynb
autonomous: true
requirements: [ENV-02, ENV-03]

must_haves:
  truths:
    - "学习者可以找到 docker-compose.yml，其中包含注释掉的 TimescaleDB + Grafana 服务及说明 Phase 2 用途的注释"
    - "学习者可以阅读 README.md 中的数据字典，列出所有列、类型、描述和示例值"
    - "学习者可以按照 docs/chinese-electricity-data-guide.md 从和鲸/天池/地方平台手动下载中国电力数据"
    - "每个 notebook 在主要内容之后的 markdown cell 中都有思考题"
    - "每个 notebook 对缺失数据都有错误处理，显示清晰的用户友好消息"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Phase 2 TimescaleDB + Grafana 服务的注释骨架 YAML"
      contains: "timescaledb"
    - path: "README.md"
      provides: "完整项目概览 + 含所有列 schema 的数据字典 + 搭建说明"
      min_lines: 60
      contains: "数据字典"
    - path: "docs/chinese-electricity-data-guide.md"
      provides: "中国电力数据平台逐步手动下载指南"
      contains: "和鲸"
  key_links:
    - from: "docker-compose.yml"
      to: "Phase 2 ASSUME 可视化"
      via: "说明 TimescaleDB + Grafana 用途的注释"
    - from: "README.md 数据字典"
      to: "pipeline/data_loader.py OUTPUT_SCHEMA"
      via: "列名与 DataLoader 输出匹配"
    - from: "docs/chinese-electricity-data-guide.md"
      to: "notebooks/01_data_ingestion.ipynb §1.2"
      via: "在 notebook 中作为下载说明来源被引用"
---

## Phase 目标

**作为**一名学习者，**我希望**找到项目文档（README 数据字典、中国数据获取指南）、看到经过润色的 Jupyter notebook（含思考题和错误处理），并找到 Phase 2 的 Docker Compose 骨架，**以便**我拥有一个完整的、自文档化的、可以独立导航的学习平台。

<objective>
将 Phase 1 交付物润色至可用于学习的状态。创建 Docker Compose 骨架 YAML（注释掉的 TimescaleDB + Grafana，为 Phase 2 ASSUME 可视化做准备 — 按 D-07）。增强 README 添加完整数据字典。编写中国电力数据获取指南（docs/chinese-electricity-data-guide.md）附带手动下载说明。为所有 5 个 Jupyter notebook 添加思考题和错误处理。这是最终的"收尾"计划 — 所有核心功能已在 Plan 01-01 和 01-02 中构建完成。

目的：将可运行代码转化为完整的学习平台。新学习者应能阅读 README.md、按照说明搭建环境、按指南下载数据，并通过全部 5 个含思考题的 notebook 学习。

产出：8 个文件（1 个新 YAML、1 个新 docs markdown、5 个 notebook 增强、1 个 README 增强）。
</objective>

<execution_context>
@/mnt/e/Ellectric/.opencode/get-shit-done/workflows/execute-plan.md
@/mnt/e/Ellectric/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@.planning/phases/01-data-foundation-basic-prediction/01-CONTEXT.md

## 前置计划产出（来自 01-01 和 01-02）
@.planning/phases/01-data-foundation-basic-prediction/01-01-SUMMARY.md
@.planning/phases/01-data-foundation-basic-prediction/01-02-SUMMARY.md
> 注意：加载两份 SUMMARY 以获取确切文件内容、列名、函数签名和模块结构。Notebook 01-05 已存在；本计划对其进行增强。

## 关键决策
- **D-07**: Docker Compose 可选 — Phase 1 不需要。仅创建骨架 YAML，服务已注释，标记为"Phase 2 依赖"。
- **D-03**: 中国数据手动下载工作流 — 清晰记录步骤
- **D-11**: Notebook 命名规范已实现（01_data_ingestion 至 05_end_to_end_baseline）
- **D-15**: plotly 是可视化库 — 确保所有 notebook 可视化使用 plotly

## 来自 RESEARCH.md 的上下文
- **中国数据平台**（RESEARCH.md §1）：和鲸社区、阿里天池、地方开放数据平台（深圳/上海/广东/菏泽）— 均需浏览器手动访问
- **ENV-02**: Docker Compose for TimescaleDB + Grafana（Phase 2 ASSUME 可视化依赖）
- **ENV-03**: Jupyter notebook 结构：markdown → code → visualization → reflection
</context>

<tasks>

<task type="auto" tdd="false">
  <name>任务 1：Docker Compose 骨架（docker-compose.yml）</name>
  <files>docker-compose.yml</files>
  <read_first>
    @.planning/phases/01-data-foundation-basic-prediction/01-CONTEXT.md §D-07（仅骨架，标记为 Phase 2 依赖）
    @.planning/REQUIREMENTS.md §ENV-02（Docker Compose for TimescaleDB + Grafana）
    @.planning/research/STACK.md §Supporting Infrastructure（Docker、TimescaleDB、Grafana 参考）
  </read_first>
  <action>
    在项目根目录创建 `docker-compose.yml`，按 D-07 和 ENV-02。所有服务均已**注释** — 这是 Phase 2 激活用的骨架。

    文件结构（YAML 中使用 `#` 写大量 YAML 注释）：

    ```yaml
    # =============================================================================
    # Ellectric — Docker Compose 骨架
    # =============================================================================
    # 以下服务 Phase 1 不需要。
    # 在 Phase 2 引入 ASSUME 市场仿真时取消注释并运行。
    # ASSUME 使用 TimescaleDB 存储数据，Grafana 展示仪表板。
    #
    # Phase 2 用法:
    #   docker compose up -d
    #   docker compose down
    # =============================================================================
    ```

    服务定义（全部用每行 `# ` 前缀注释）：

    **timescaledb 服务**（已注释）:
    - image: `timescale/timescaledb:latest-pg16`
    - container_name: `ellectric-timescaledb`
    - ports: `"5432:5432"`
    - environment: `POSTGRES_USER=ellectric`, `POSTGRES_PASSWORD=ellectric`, `POSTGRES_DB=assume`
    - volumes: `timescaledb_data:/var/lib/postgresql/data`
    - healthcheck: `pg_isready -U ellectric -d assume`
    - restart: `unless-stopped`

    **grafana 服务**（已注释）:
    - image: `grafana/grafana:latest`
    - container_name: `ellectric-grafana`
    - ports: `"3000:3000"`
    - environment: `GF_SECURITY_ADMIN_USER=admin`, `GF_SECURITY_ADMIN_PASSWORD=admin`
    - depends_on: `timescaledb`
    - volumes: `grafana_data:/var/lib/grafana`
    - restart: `unless-stopped`

    **volumes**（已注释）:
    - `timescaledb_data:`
    - `grafana_data:`

    在注释的服务上方，添加醒目的未注释头块：
    ```
    # 以下所有服务已注释 — 这是 Phase 2 的骨架。
    # 取消你需要的服务注释，然后运行: docker compose up -d
    # Phase 1 不需要任何 Docker 服务 — 一切通过 Jupyter 本地运行。
    ```
  </action>
  <verify>
    <automated>bash -c 'test -f docker-compose.yml && echo "文件存在" && grep -c "timescaledb" docker-compose.yml && grep -c "grafana" docker-compose.yml && echo "两个服务均已包含"'</automated>
    <automated>python3 -c "
with open('docker-compose.yml') as f:
    content = f.read()
    assert 'timescale/timescaledb' in content, '缺少 TimescaleDB 镜像'
    assert 'grafana/grafana' in content, '缺少 Grafana 镜像'
    assert 'Phase 2' in content, '缺少 Phase 2 引用'
    assert 'Phase 1' in content and '不需要' in content, '缺少 Phase 1 说明'
    assert '5432:5432' in content, '缺少 timescaledb 端口'
    assert '3000:3000' in content, '缺少 grafana 端口'
    # 验证服务已注释（头块之后的行以 # 开头）
    lines = content.split('\n')
    service_lines = [l for l in lines if 'timescaledb:' in l or 'grafana:' in l or 'volumes:' in l]
    for line in service_lines:
        stripped = line.strip()
        if stripped.startswith('timescaledb:') or stripped.startswith('grafana:') or stripped.startswith('volumes:'):
            assert line.strip().startswith('#'), f'预期注释行，实际: {line[:80]}'
    print('Docker Compose 骨架已验证')
"</automated>
  </verify>
  <done>
    - `docker-compose.yml` 在项目根目录存在，含 timescaledb 和 grafana 服务定义
    - 两个服务及其 volumes 均以 `#` 前缀注释
    - 文件含头块说明这是 Phase 2 骨架（Phase 1 不需要 Docker）
    - TimescaleDB 配置端口 5432，Grafana 端口 3000
    - 所有环境变量使用 `ellectric` / `admin` 作为默认值（学习平台 — 无安全问题）
  </done>
</task>

<task type="auto" tdd="false">
  <name>任务 2：README 数据字典 + 中国数据获取指南（README.md + docs/）</name>
  <files>README.md, docs/chinese-electricity-data-guide.md</files>
  <read_first>
    @.planning/phases/01-data-foundation-basic-prediction/01-CONTEXT.md §D-03（手动下载工作流文档）、§D-05（标准化列 schema）
    @.planning/phases/01-data-foundation-basic-prediction/01-RESEARCH.md §1（中国数据平台 — 和鲸、天池、地方平台、URL、访问结果）
    @README.md（Plan 01-01 中已存在 — 增强，不覆盖项目标题/快速开始部分）
  </read_first>
  <action>
    **增强项目根目录的 `README.md`**（追加新部分；保留 Plan 01-01 中已有的标题、Phase 1 badge、描述和快速开始部分）：

    在快速开始部分之后添加 `## 数据字典 (Data Dictionary)` 部分：

    ```markdown
    ## 数据字典 (Data Dictionary)

    ### 标准化 Schema（所有 DataLoader）

    | 列 | 类型 | 描述 | 示例值 |
    |--------|------|-------------|----------------|
    | `timestamp` | `datetime64[ns, UTC]` | UTC 时间戳（timezone-aware） | `2023-01-01T00:00:00+00:00` |
    | `load_mw` | `float64` | 电力负荷/需求，单位兆瓦 (MW) | `523000.5` |
    | `data_source` | `str` | 数据来源 | `OWID`, `chinese_platform` |
    | `region` | `str` | 地理区域 | `China`, `Guangdong` |

    ### OWID 中国年度数据

    | 列 | 描述 | 单位 | 示例 |
    |--------|-------------|------|---------|
    | `year` | 日历年 | — | 2023 |
    | `electricity_generation` | 中国总发电量 | TWh | 9,456.4 |
    | `electricity_demand` | 总用电需求（标准化 schema 中映射为 `load_mw`） | TWh | 9,200.0 |
    | `coal_electricity` | 煤电发电量 | TWh | 5,200.0 |
    | `solar_electricity` | 光伏发电量 | TWh | 580.0 |
    | `wind_electricity` | 风电发电量 | TWh | 880.0 |

    ### 中国手动下载数据

    | 列 | 描述 | 单位 | 是否必需 |
    |--------|-------------|------|----------|
    | `timestamp` | 测量时间（本地时间，自动转换为 UTC） | datetime | ✅ |
    | `load_mw` | 电力负荷 | MW | ✅ |
    | `region` | 省份或城市（可选，默认 `China`） | str | ❌ |

    > **注意：** 标准化 schema 输出（DataLoader 处理后）始终使用 `timestamp` (UTC) 和 `load_mw` (MW)。
    > 来自中国平台的输入文件可能使用不同的列名 — ChineseDataLoader 会自动映射
    > （例如 `时间` → `timestamp`，`负荷` → `load_mw`）。
    ```

    添加 `## 项目结构 (Project Structure)` 部分：

    展示 Phase 1 完成后的目录树：
    ```
    ellectric/
    ├── pipeline/           # 可复用的 Python 模块
    │   ├── __init__.py
    │   ├── data_loader.py  # DataLoader ABC + OWIDChinaLoader + ChineseDataLoader
    │   ├── cleaner.py      # 数据清洗管道
    │   ├── features.py     # 特征工程（Tier 1-3）
    │   └── forecaster.py   # 持久性预测 + XGBoostForecaster
    ├── notebooks/          # Jupyter notebook（顺序学习路径）
    │   ├── 01_data_ingestion.ipynb
    │   ├── 02_data_cleaning.ipynb
    │   ├── 03_feature_engineering.ipynb
    │   ├── 04_load_forecasting.ipynb
    │   └── 05_end_to_end_baseline.ipynb
    ├── data/               # 原始数据存储（gitignored）
    ├── docs/               # 文档
    │   └── chinese-electricity-data-guide.md
    ├── models/             # 训练模型持久化（gitignored）
    ├── setup.sh            # 一键环境搭建
    ├── requirements.txt    # 固定版本的 Python 依赖
    ├── docker-compose.yml  # Phase 2 骨架（已注释）
    └── README.md
    ```

    添加 `## 学习路径 (Learning Path)` 部分：
    - 简要描述 5 个 notebook 的顺序
    - 建议顺序：01 → 02 → 03 → 04 → 05
    - 注意：可以先运行 05（E2E 基线）快速获得成果，再回头从 01 开始

    添加 `## Phase 2 预览 (Phase 2 Preview)` 部分，引用 docker-compose.yml 以及 ASSUME/OpenSTEF 将添加什么。

    ---

    **创建 `docs/chinese-electricity-data-guide.md`**，按 D-03：

    内容结构：

    1. **标题**：`# Chinese Electricity Data Acquisition Guide（中国电力数据获取指南）`，副标题："How to manually download Chinese electricity load data for the Ellectric learning platform."

    2. **为什么需要手动下载？** 部分：解释中国政府开放数据平台（深圳/上海/广东/菏泽）需要浏览器交互（验证码、登录等），无法程序化访问。OWID 提供可自动获取的年度宏观数据；小时/日度数据需要手动获取。

    3. **推荐平台** 表格：

    | 平台 | URL | 数据类型 | 访问方式 | 备注 |
    |----------|-----|-----------|---------------|-------|
    | 和鲸社区 (Heywhale) | https://www.heywhale.com/ | 小时级负荷、发电量 | 免费注册 → 搜索"电力负荷" → 下载数据集 | 国内最活跃的数据科学社区；搜索"电力"、"负荷预测" |
    | 阿里天池 (Tianchi) | https://tianchi.aliyun.com/ | 各类 | 免费注册 → 竞赛数据集 → 搜索"电力" | 寻找电力预测竞赛数据集 |
    | 深圳市政府数据开放平台 | https://opendata.sz.gov.cn/ | 区域负荷 | 免费注册 + 实名验证 | 可能有区县级负荷数据 |
    | 广东省公共数据开放平台 | https://data.gd.gov.cn/ | 区域负荷 | 免费注册 | 省级数据 |
    | 菏泽市公共数据开放网 | （搜索最新网址） | 本地负荷 | 免费注册 | 市级数据，地方开放数据典范 |

    4. **逐步下载说明**（以和鲸社区为主要示例）：
       - 步骤 1：访问 https://www.heywhale.com/
       - 步骤 2：注册免费账号（邮箱或微信）
       - 步骤 3：搜索"电力负荷"或"电力数据"
       - 步骤 4：浏览结果，寻找含时间戳和 MW 值的数据集
       - 步骤 5：下载 CSV 或 Excel 文件
       - 步骤 6：将文件放入 `ellectric/data/` 目录

    5. **预期文件格式** 部分：
       - 最低必需列：一个时间戳列和一个负荷值列（MW）
       - 接受的格式：CSV（`.csv`）、Excel（`.xlsx`）、Parquet（`.parquet`）
       - `ChineseDataLoader` 按文件扩展名自动检测格式
       - 如果列名不同（例如中文列名如 `时间`、`负荷`），使用前将它们重命名为 `timestamp` 和 `load_mw`，或者 ChineseDataLoader 会尝试对常见中文列名自动映射。

    6. **验证** 部分：
       ```python
       from pipeline.data_loader import ChineseDataLoader
       loader = ChineseDataLoader(data_path='data/your_file.csv')
       df = loader.load_data()
       print(df.head())
       print(loader.get_metadata())
       ```

    7. **备选方案：OWID 年度数据** 部分：如果你找不到中国小时级数据，`OWIDChinaLoader` 提供可自动获取的中国年度能源数据 — 这足以学习管道基础知识。
  </action>
  <verify>
    <automated>bash -c 'test -f README.md && test -f docs/chinese-electricity-data-guide.md && echo "两个文件均存在"'</automated>
    <automated>python3 -c "
# 验证 README 各部分
with open('README.md') as f:
    content = f.read()
    assert '数据字典' in content or 'Data Dictionary' in content, '缺少数据字典部分'
    assert '项目结构' in content or 'Project Structure' in content, '缺少项目结构部分'
    assert 'timestamp' in content and 'load_mw' in content, '缺少标准化 schema 列'
    assert 'OWID' in content, '缺少 OWID 数据源引用'
    assert 'setup.sh' in content, '缺少 setup.sh 引用'
    print('README.md 已验证: 数据字典 + 项目结构 均已包含')

# 验证中国数据指南
with open('docs/chinese-electricity-data-guide.md') as f:
    guide = f.read()
    assert '和鲸' in guide, '缺少和鲸引用'
    assert '天池' in guide, '缺少天池引用'
    assert 'download' in guide.lower(), '缺少下载说明'
    assert 'ChineseDataLoader' in guide, '缺少 DataLoader 引用'
    assert '.csv' in guide or 'CSV' in guide, '缺少 CSV 格式说明'
    print('docs/chinese-electricity-data-guide.md 已验证')
"</automated>
  </verify>
  <done>
    - README.md 有 `## 数据字典` 部分，含标准化 schema（timestamp、load_mw、data_source、region 类型和示例）及 OWID 特定列
    - README.md 有 `## 项目结构` 部分，含目录树
    - README.md 有 `## 学习路径` 部分，含 notebook 顺序（01→05）
    - `docs/chinese-electricity-data-guide.md` 列出和鲸、天池、深圳、广东、菏泽作为数据来源并附 URL
    - 指南含和鲸社区的逐步下载说明
    - 指南说明预期文件格式（CSV/Excel/Parquet 含 timestamp + load_mw）
    - 指南含使用 ChineseDataLoader 的验证代码片段
    - 指南提及 OWID 年度数据作为备选方案
  </done>
</task>

<task type="auto" tdd="false">
  <name>任务 3：Notebook 润色 — 所有 5 个 notebook 添加思考题 + 错误处理</name>
  <files>
    notebooks/01_data_ingestion.ipynb,
    notebooks/02_data_cleaning.ipynb,
    notebooks/03_feature_engineering.ipynb,
    notebooks/04_load_forecasting.ipynb,
    notebooks/05_end_to_end_baseline.ipynb
  </files>
  <read_first>
    @.planning/REQUIREMENTS.md §ENV-03（notebook 结构：markdown → code → visualization → reflection）
    @.planning/phases/01-data-foundation-basic-prediction/01-CONTEXT.md §D-10（轻量 notebook、厚重模块）、§D-11（命名规范）
    @.planning/phases/01-data-foundation-basic-prediction/01-01-SUMMARY.md（获取已创建的 notebook 内容）
    @.planning/phases/01-data-foundation-basic-prediction/01-02-SUMMARY.md（获取已创建的 notebook 内容）
  </read_first>
  <action>
    **增强全部 5 个 notebook**，按 ENV-03（markdown → code → visualization → 思考题）和 D-10（轻量 notebook）。**先阅读**每个 notebook 以了解其当前结构，然后添加或增强以下内容：

    ---

    **所有 5 个 notebook 通用增强：**

    1. **标题 markdown cell 增强**：在标题后添加学习目标（项目符号列表）。格式：
       ```
       # 0X — 标题

       **学习目标 (Learning Objectives):**
       - 目标 1
       - 目标 2
       - 目标 3
       ```

    2. **数据加载 cell 的错误处理**：将每个 notebook 中第一次管道调用的数据加载代码包裹如下：
       ```python
       try:
           loader = OWIDChinaLoader()
           df = loader.load_data()
           print(f"✅ 数据已加载: {len(df)} 行")
       except Exception as e:
           print(f"❌ 加载 OWID 数据失败: {e}")
           print("请检查网络连接，或尝试从 data/owid_energy_raw.csv 加载缓存数据")
           print("如果你有中国小时级数据，请使用 ChineseDataLoader(data_path='data/your_file.csv') 替代。")
       ```

    3. **思考题部分 (思考题)**：确保每个 notebook **以** 标题为 `### 💭 思考题 (Reflection Questions)` 的 markdown cell **结尾**，包含 2-4 个问题。问题应：
       - 联系真实电力交易场景
       - 考察对陷阱的理解（look-ahead bias、spike 是信号）
       - 关联 Phase 1 学习目标
       - 可以从刚刚在 notebook 输出中看到的内容来回答

    4. **Cell 编号**：给每个主要部分的 markdown cell 编号（例如 `### Step 1: xxx`、`### Step 2: xxx`），使 notebook 读起来像引导式教程。

    ---

    **各 notebook 特定增强：**

    **01_data_ingestion.ipynb** — 需要**添加**的思考题（如果尚未存在）：
    1. OWID 数据是年度数据 — 这对短期负荷预测有什么限制？
    2. 与美国 PUDL 数据相比，为什么获取中国小时级电力数据更难？
    3. 如果你有广东省的小时级负荷数据，你会如何构建 DataLoader？

    **02_data_cleaning.ipynb** — 需要**添加**的思考题：
    1. 在电力数据中为什么报告离群值而不是删除它们？（提示：想想夏季用电高峰）
    2. 如果用仍含缺失时间戳的数据训练模型会发生什么？
    3. 如果你在工作日中午看到一个负荷值为 0 MW，你会相信它吗？你会调查什么？

    **03_feature_engineering.ipynb** — 需要**添加**的思考题：
    1. `lag_168h` 特征使用上周的负荷。为什么这对负荷预测是一个强信号？
    2. 我们不在 `FeatureEngineer` 内部缩放特征 — 为什么缩放必须在预测器的训练循环内部进行？
    3. 如果你有天气数据（温度、云量），你会设计哪些额外特征？

    **04_load_forecasting.ipynb** — 需要**添加**的思考题：
    1. MAE 在 5 折之间会变化 — 这反映了时序数据的什么特性（非平稳性）？
    2. TimeSeriesSplit 中的 `gap=24` 参数防止了什么？（想想 lag-24h 特征。）
    3. XGBoost 与 Notebook 05 的持久性基线相比表现如何？你更信任哪个？
    4. 如果用小时级数据而非年度数据训练该模型，哪些地方会改变？

    **05_end_to_end_baseline.ipynb** — 需要**添加**的思考题：
    1. 累计 P&L 为正意味着策略在这个简化仿真中盈利。哪些现实成本被遗漏了？
    2. 持久性预测假设"明天 = 昨天"。这个假设在什么时候会完全失效？
    3. 本 notebook 使用 OWID 年度数据（每年 1 个数据点）。全年小时级数据（8,760 个点）会如何改变你能做的分析深度？

    ---

    **额外润色项**（缺失时应用）：

    - **导入合并**：确保所有导入在 notebook 顶部一个 cell 中（notebook 的 PEP 8 风格）
    - **plotly 渲染**：如果尚未存在，在早期 cell 中添加 `pio.renderers.default = 'notebook'`
    - **每个 notebook 的最后一个 cell**：添加含 `---` 分隔线和"下一个 notebook: [0X_name]"链接的 markdown cell
    - **数据来源归属**：在数据加载后添加 markdown cell，注明数据来源（OWID → Our World in Data, CC BY 4.0；中国平台 → 见指南）
  </action>
  <verify>
    <automated>cd /mnt/e/Ellectric && source .venv/bin/activate && python -c "
import nbformat
import os

notebook_dir = 'notebooks'
expected_notebooks = [
    '01_data_ingestion.ipynb',
    '02_data_cleaning.ipynb',
    '03_feature_engineering.ipynb',
    '04_load_forecasting.ipynb',
    '05_end_to_end_baseline.ipynb'
]

for nb_name in expected_notebooks:
    nb_path = os.path.join(notebook_dir, nb_name)
    assert os.path.exists(nb_path), f'缺少 notebook: {nb_path}'
    
    with open(nb_path) as f:
        nb = nbformat.read(f, as_version=4)
    
    # 转为文本进行关键词搜索
    text = ' '.join(cell.source for cell in nb.cells)
    
    # 检查思考题部分
    assert '思考题' in text, f'{nb_name}: 缺少思考题部分'
    assert 'Reflection' in text, f'{nb_name}: 缺少 Reflection 部分'
    
    # 检查错误处理
    assert 'try' in text.lower() or 'except' in text.lower() or 'try:' in text, \
        f'{nb_name}: 缺少 try/except 错误处理'
    
    # 检查学习目标
    assert '学习目标' in text or 'Learning Objectives' in text, \
        f'{nb_name}: 缺少学习目标'
    
    # 检查相关 notebook 中的 plotly renderer
    if nb_name in ['04_load_forecasting.ipynb', '05_end_to_end_baseline.ipynb']:
        assert 'pio.renderers' in text or 'plotly' in text.lower(), \
            f'{nb_name}: 缺少 plotly renderer 设置'
    
    print(f'{nb_name}: ✅ {len(nb.cells)} cells — 思考题 + 错误处理 + 学习目标 均已包含')

print('全部 5 个 notebook 已验证')
"</automated>
  </verify>
  <done>
    - 全部 5 个 notebook 在末尾有 `### 💭 思考题 (Reflection Questions)` markdown cell，含 2-4 个问题
    - 全部 5 个 notebook 在标题后有学习目标
    - 全部 5 个 notebook 在数据加载处有 try/except 错误处理，显示用户友好消息
    - Plotly notebook（04、05）已配置 `pio.renderers.default = 'notebook'`
    - 每个 notebook 的导入在顶部合并
    - 每个 notebook 末尾有"下一个 notebook"链接（05 例外，显示"Phase 1 结束"）
    - Notebook 仍可从顶部到底部无错误执行（保留现有功能）
  </done>
</task>

</tasks>

<threat_model>
## 信任边界
未引入新的信任边界。Docker Compose 文件已注释，无实际作用。

| 边界 | 描述 |
|----------|-------------|
| 无新增 | 所有工作为文档和 notebook 增强 |

## STRIDE 威胁登记

| 威胁 ID | 类别 | 组件 | 处置 | 缓解计划 |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Information Disclosure | ChineseDataLoader 指南列出公开 URL | accept | 所有 URL 均为众所周知的政府开放数据门户 — 无凭据暴露风险。 |
| T-01-03-02 | Tampering | docker-compose.yml | accept | 文件仅供引用。所有服务已注释 — 无服务启动。可接受。 |
</threat_model>

<verification>
在任务 3 后运行 notebook 验证：

```bash
cd /mnt/e/Ellectric && source .venv/bin/activate && python -c "
import nbformat, os

notebook_dir = 'notebooks'
expected = [
    '01_data_ingestion.ipynb',
    '02_data_cleaning.ipynb',
    '03_feature_engineering.ipynb',
    '04_load_forecasting.ipynb',
    '05_end_to_end_baseline.ipynb'
]

all_ok = True
for nb_name in expected:
    path = os.path.join(notebook_dir, nb_name)
    with open(path) as f:
        nb = nbformat.read(f, as_version=4)
    text = ' '.join(cell.source for cell in nb.cells)
    
    checks = {
        '学习目标': '学习目标' in text or 'Learning Objectives' in text,
        '思考题': '思考题' in text,
        '错误处理': 'try' in text or 'except' in text,
        '非空': len(nb.cells) >= 3
    }
    
    status = '✅' if all(checks.values()) else '❌'
    failed = [k for k, v in checks.items() if not v]
    print(f'{status} {nb_name}: {len(nb.cells)} cells', end='')
    if failed:
        print(f' — 缺失: {failed}')
        all_ok = False
    else:
        print()

if all_ok:
    print('所有 notebook 通过验证')
else:
    print('部分 notebook 有缺失元素 — 见上方')
"

# 额外检查:
echo "---" && echo "文件存在性检查:" && ls -la docker-compose.yml docs/chinese-electricity-data-guide.md README.md && echo "所有 Plan 01-03 文件均存在"
```
</verification>

<success_criteria>
- [ ] `docker-compose.yml` 存在，含注释掉的 timescaledb + grafana 服务，标记为"Phase 2 骨架"
- [ ] `README.md` 有数据字典部分，含标准化 schema（timestamp、load_mw、data_source、region 的类型和示例）
- [ ] `README.md` 有项目结构部分，显示目录树
- [ ] `docs/chinese-electricity-data-guide.md` 列出 5 个中国数据平台，附 URL 和和鲸逐步说明
- [ ] 全部 5 个 notebook 在标题后有学习目标 (学习目标)
- [ ] 全部 5 个 notebook 末尾有思考题 (思考题) 部分，含 2-4 个问题
- [ ] 全部 5 个 notebook 在数据加载操作处有 try/except 错误处理
- [ ] Notebook 04 和 05 已配置 `pio.renderers.default = 'notebook'`
- [ ] 现有 notebook 功能未被破坏 — notebook 仍可从顶部到底部执行
- [ ] Docker Compose 文件（取消注释后）作为语法正确 YAML 验证通过
</success_criteria>

<output>
完成后创建 `.planning/phases/01-data-foundation-basic-prediction/01-03-SUMMARY.md`
</output>
