---
description: Greenfield project initialization — deep questions, research, requirements, roadmap
argument-hint: "[--dir <path>] [--interactive]"
tools:
  read: true
  write: true
  edit: true
  bash: true
  glob: true
  agent: true
  question: true
  webfetch: true
---
Initialize a new project with SillySpec:

- `sillyspec init --dir <path>` — non-interactive, auto-detect tool
- `sillyspec init --interactive` — guided setup
- `sillyspec setup` — install recommended MCP tools
