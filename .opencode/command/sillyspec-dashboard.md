---
description: Start SillySpec Dashboard Web UI
argument-hint: "[--port <number>] [--no-open]"
tools:
  read: true
  bash: true
---
Run `sillyspec dashboard $ARGUMENTS` to start the Dashboard Web UI.

- `--port <number>` — custom port (default 3456)
- `--no-open` — don't auto-open browser
