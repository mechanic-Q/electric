# Ellectric — 核心依赖
# 安装: pip install -r requirements.txt
# 国内镜像: pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# ── 数据处理 ──────────────────────────────────────────
pandas==3.0.3
numpy>=2.0.0
pyarrow==22.0.0

# ── 机器学习（含强化学习） ──────────────────────────
scikit-learn==1.8.0
xgboost==3.2.0
gymnasium==1.2.3          # RL 环境 API 标准（<1.3.0 兼容 sb3）
stable-baselines3==2.8.0  # PPO/SAC/TD3 强化学习算法

# ── 可视化（交互式 + 可解释性） ──────────────────────
plotly==6.7.0
shap>=0.46                # 模型可解释性（SHAP 值）

# ── 开发环境 ──────────────────────────────────────────
jupyter==1.1.1
nbformat>=5.0.0
jedi>=0.19.1,<0.20    # shap 兼容性 pin（0.20+ 与 IPython completer 冲突）
tensorboard==2.18.0       # RL 训练监控（奖励/行为曲线）

# ── Phase 4: Integration + LLM Interface ──
-r requirements-phase4.txt
