---
phase: 01-data-foundation-basic-prediction
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - pipeline/features.py
  - pipeline/forecaster.py
  - pipeline/cleaner.py
  - pipeline/data_loader.py
  - pipeline/__init__.py
  - notebooks/02_data_cleaning.ipynb
  - notebooks/03_feature_engineering.ipynb
  - notebooks/04_load_forecasting.ipynb
autonomous: true
requirements: [DATA-03, DATA-04, PRED-01, VIZ-01]

must_haves:
  truths:
    - "Learner can generate progressive calendar + lag + rolling window features (Tiers 1-3) from timestamp and load_mw"
    - "Learner can train an XGBoost model with TimeSeriesSplit and see per-fold MAE values"
    - "Learner can view an interactive actual-vs-predicted line overlay and error distribution histogram via plotly"
    - "Scaler is fit ONLY on training data inside each TimeSeriesSplit fold (no look-ahead bias)"
    - "ChineseDataLoader fully implements load_hourly_demand, data version tracking, and metadata"
  artifacts:
    - path: "pipeline/features.py"
      provides: "FeatureEngineer with calendar (hour/dow/month/is_weekend), lag (24h/168h), rolling (mean/std 24h)"
      exports: ["FeatureEngineer"]
      min_lines: 80
    - path: "pipeline/forecaster.py"
      provides: "XGBoostForecaster with train_evaluate (TimeSeriesSplit), save_model/load_model (joblib), plot_forecast"
      exports: ["persistence_forecast", "calculate_pnl", "plot_pnl", "XGBoostForecaster"]
    - path: "notebooks/04_load_forecasting.ipynb"
      provides: "XGBoost training + MAE evaluation + actual-vs-predicted + error histogram via plotly"
  key_links:
    - from: "pipeline/forecaster.py XGBoostForecaster.train_evaluate()"
      to: "pipeline/features.py FeatureEngineer.generate_all_features()"
      via: "generates features, then fits scaler on X_train ONLY in each fold"
      pattern: "feature_engineer\\.generate_all_features"
    - from: "pipeline/forecaster.py train_evaluate()"
      to: "sklearn TimeSeriesSplit(n_splits=5, gap=24)"
      via: "ensures train_idx always precedes test_idx temporally"
      pattern: "TimeSeriesSplit\\(n_splits=5, gap=24\\)"
    - from: "pipeline/forecaster.py train_evaluate()"
      to: "StandardScaler().fit_transform(X_train)"
      via: "fit ONLY on training slice; transform test slice separately"
      pattern: "\\.fit_transform\\(X_train"
    - from: "pipeline/data_loader.py ChineseDataLoader.load_data()"
      to: "local CSV/Parquet files in data/"
      via: "pd.read_csv or pd.read_parquet, validates columns, standardizes timestamps"
      pattern: "pd\\.read_(csv|parquet)"
    - from: "notebooks/03_feature_engineering.ipynb"
      to: "pipeline/features.py FeatureEngineer"
      via: "generates Tier 1→2→3 features progressively, displays result head()"
      pattern: "FeatureEngineer"
---

## Phase Goal

**As a** learner, **I want to** engineer time-series features, train an XGBoost load forecasting model with proper temporal validation (no look-ahead bias), and see interactive prediction-vs-actual charts, **so that** I understand how to build and evaluate a real ML forecasting pipeline.

<objective>
Transform the working Walking Skeleton (Plan 01-01) into a proper ML forecasting pipeline. Add progressive feature engineering (calendar + lag + rolling window features), train an XGBoost model with strict TimeSeriesSplit validation and scaler encapsulation (to prevent look-ahead bias — Pitfall #1), add interactive plotly forecasting visualizations, and complete the ChineseDataLoader implementation with data versioning. Creates the remaining 3 Jupyter notebooks (02/03/04) and a new pipeline/features.py module.

Purpose: Deliver the core learning outcome of Phase 1 — a learner can open Notebook 04, train an XGBoost model with proper temporal validation, see the MAE, and visually compare predictions against actual loads. This is the "real model" that replaces the naive persistence forecast from Plan 01-01.

Output: 8 files (1 new module, 3 existing module enhancements, 3 new notebooks, __init__.py update).
</objective>

<execution_context>
@/mnt/e/Ellectric/.opencode/get-shit-done/workflows/execute-plan.md
@/mnt/e/Ellectric/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@.planning/phases/01-data-foundation-basic-prediction/01-CONTEXT.md
@.planning/phases/01-data-foundation-basic-prediction/01-RESEARCH.md

## Prior Plan Output (from 01-01)
@.planning/phases/01-data-foundation-basic-prediction/01-01-SUMMARY.md
> NOTE to executor: load this SUMMARY for exact file contents, function signatures, and patterns established in Wave 1. Use the same coding style.

## Key Decisions (from CONTEXT.md)
- **D-08**: Progressive features — Tier 1 (hour, day_of_week, month, is_weekend, lag_24h) → Tier 2 (is_holiday, lag_168h) → Tier 3 (rolling_mean_24h, rolling_std_24h)
- **D-09**: CRITICAL: All scalers fit ONLY on training data using `TimeSeriesSplit`. NEVER `fit()` on full dataset. Enforced by forecaster.py structure.
- **D-14**: MAE as the only evaluation metric. No RMSE, MAPE, sMAPE in notebook output.
- **D-15**: plotly for interactive visualizations (actual-vs-predicted overlay, error histogram)

## Research Output (from RESEARCH.md)
- **TimeSeriesSplit config**: `TimeSeriesSplit(n_splits=5, gap=24)` — gap prevents autocorrelation leakage from 24h lag features
- **Scaler pattern**: encapsulate StandardScaler inside `train_evaluate()` loop — fit on X_train, transform X_test per fold
- **XGBoost defaults**: `n_estimators=100, max_depth=6, learning_rate=0.1, subsample=0.8, colsample_bytree=0.8, random_state=42`
- **Feature tiers**: per RESEARCH.md §3
- **DataLoader full spec**: per RESEARCH.md §5 (load_hourly_demand, metadata, data_version)
- **Plotly patterns**: per RESEARCH.md §4 (overlay, histogram, residuals)
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Feature engineering module (pipeline/features.py) + Notebook 03</name>
  <files>pipeline/features.py, notebooks/03_feature_engineering.ipynb</files>
  <read_first>
    @.planning/phases/01-data-foundation-basic-prediction/01-CONTEXT.md §D-08 (progressive features), §D-09 (scaler fit-on-train)
    @.planning/phases/01-data-foundation-basic-prediction/01-RESEARCH.md §3 (feature tiers, TimeSeriesSplit config, scaler pattern)
    @.planning/research/PITFALLS.md §Pitfall 1 (look-ahead bias) and §Pitfall 7 (feature leakage)
    @pipeline/forecaster.py (from Plan 01-01 SUMMARY — understand existing function structure, persistence_forecast signature)
  </read_first>
  <action>
    Create `pipeline/features.py` — FeatureEngineer class implementing D-08 progressive feature engineering with D-09/Pitfall 1 enforcement:

    **Class: FeatureEngineer**
    - `__init__(self, scaler: StandardScaler | None = None)`:
      - If `scaler` is provided, use it (enables forecaster to pass a pre-fit scaler). If None, creates `StandardScaler()` internally.
      - Store scaler as `self.scaler`. CRITICAL: never call `fit()` or `fit_transform()` inside FeatureEngineer — scaling is the forecaster's responsibility. FeatureEngineer only produces unscaled feature columns. The scaler is stored for reference but NOT self-applied.

    - `generate_calendar_features(df: pd.DataFrame) -> pd.DataFrame` (Tier 1 per D-08):
      - Extract from `df['timestamp']`:
        - `hour` (int, 0-23, via `df['timestamp'].dt.hour`)
        - `day_of_week` (int, 0=Monday, 6=Sunday, via `.dt.dayofweek`)
        - `month` (int, 1-12, via `.dt.month`)
        - `is_weekend` (int, 1 if Saturday-Sunday else 0, via `.dt.dayofweek >= 5`)
      - Return DataFrame with only these feature columns (do not modify original df).

    - `generate_lag_features(df: pd.DataFrame, lag_hours: list[int] = [24, 168]) -> pd.DataFrame` (Tier 2):
      - For each `lag` in `lag_hours`: create column `lag_{lag}h` = `df['load_mw'].shift(lag)`.
      - Fill leading NaN from shift with `fillna(method='bfill')`.
      - Return DataFrame with lag feature columns.

    - `generate_rolling_features(df: pd.DataFrame, windows: list[int] = [24]) -> pd.DataFrame` (Tier 3):
      - For each `w` in `windows`:
        - `rolling_mean_{w}h` = `df['load_mw'].rolling(window=w, min_periods=1).mean()`
        - `rolling_std_{w}h` = `df['load_mw'].rolling(window=w, min_periods=1).std()`
      - Fill initial NaN in rolling features (when window not full) with expanding window equivalent (already handled by min_periods=1).
      - Return DataFrame with rolling feature columns.

    - `generate_all_features(df: pd.DataFrame, tier: int = 3) -> pd.DataFrame`:
      - Tier 1: `calendar = self.generate_calendar_features(df)`. Return `calendar` joined with original `timestamp` + `load_mw`.
      - Tier 2: calendar + `lag = self.generate_lag_features(df)`. Return `pd.concat([calendar, lag], axis=1)` with timestamp+load_mw.
      - Tier 3: calendar + lag + `rolling = self.generate_rolling_features(df)`. Return `pd.concat([calendar, lag, rolling], axis=1)` with timestamp+load_mw.
      - Add `timestamp` and `load_mw` columns as the first two columns of the output DataFrame for convenience.
      - Note: features are returned UNSCALED (scaling is forecaster's job per D-09).

    - `get_feature_names(self, tier: int = 3) -> list[str]`:
      - Returns ordered list of feature column names for the given tier (excluding timestamp and load_mw).

    **Pitfall 1/7 enforcement note**: FeatureEngineer does NOT fit scalers, does NOT select features, does NOT compute statistics across the full dataset. It only creates deterministic lag/rolling/calendar columns. The forecaster handles scaling inside each TimeSeriesSplit fold.

    ---

    **notebooks/03_feature_engineering.ipynb** — demonstrates feature generation per D-08, D-10:

    Structure (each section = markdown cell + code cell(s)):
    1. Markdown: `# 03 — 特征工程 (Feature Engineering)` with learning objectives: create time-based features, understand lag and rolling features, avoid look-ahead bias.
    2. Code: `from pipeline.data_loader import OWIDChinaLoader; from pipeline.cleaner import clean_data; from pipeline.features import FeatureEngineer; import plotly.express as px`
    3. Markdown: `### Step 1: Load Cleaned Data`
    4. Code: `loader = OWIDChinaLoader(); df = loader.load_data(); df_clean = clean_data(df); print(f"Cleaned data: {df_clean.shape}"); df_clean.head()`
    5. Markdown: `### Step 2: Tier 1 — Calendar Features (5 core features per D-08)`
    6. Code: `engineer = FeatureEngineer(); df_t1 = engineer.generate_all_features(df_clean, tier=1); print(f"Tier 1 features: {engineer.get_feature_names(tier=1)}"); df_t1.head()`
    7. Markdown: `### Step 3: Tier 2 — Add Lag Features (lag-24h, lag-168h)`
    8. Code: `df_t2 = engineer.generate_all_features(df_clean, tier=2); print(f"Tier 2 features: {engineer.get_feature_names(tier=2)}"); df_t2.head()` — show how lag features shift data.
    9. Markdown: `### Step 4: Tier 3 — Add Rolling Window Statistics (rolling_mean_24h, rolling_std_24h)`
    10. Code: `df_t3 = engineer.generate_all_features(df_clean, tier=3); print(f"Tier 3 features: {engineer.get_feature_names(tier=3)}"); df_t3.head()`
    11. Markdown: `### Step 5: Feature Comparison`
    12. Code: display side-by-side comparison table: `pd.DataFrame({'Tier 1': [len(engineer.get_feature_names(1))], 'Tier 2': [len(engineer.get_feature_names(2))], 'Tier 3': [len(engineer.get_feature_names(3))]}, index=['Feature Count'])`
    13. Markdown: `### 思考题 (Reflection)` with questions: (a) Why do we NOT scale features inside the feature engineering step? (b) What information does lag-168h (one week ago) capture about electricity demand? (c) Why is it dangerous to compute rolling statistics over the full dataset before splitting?
  </action>
  <verify>
    <automated>cd /mnt/e/Ellectric && source .venv/bin/activate && python -c "
from pipeline.data_loader import OWIDChinaLoader
from pipeline.cleaner import clean_data
from pipeline.features import FeatureEngineer

# Load and clean data
loader = OWIDChinaLoader()
df = loader.load_data()
df_clean = clean_data(df)

# Tier 1 features
eng = FeatureEngineer()
df_t1 = eng.generate_all_features(df_clean, tier=1)
feat_t1 = eng.get_feature_names(tier=1)
assert len(feat_t1) == 4, f'Tier 1 should have 4 features, got {len(feat_t1)}'
assert 'hour' in feat_t1 or 'day_of_week' in feat_t1, 'Missing calendar features'
assert 'load_mw' in df_t1.columns, 'Missing load_mw target column'

# Tier 2 features
df_t2 = eng.generate_all_features(df_clean, tier=2)
feat_t2 = eng.get_feature_names(tier=2)
assert len(feat_t2) >= 6, f'Tier 2 should have >=6 features, got {len(feat_t2)}'

# Tier 3 features
df_t3 = eng.generate_all_features(df_clean, tier=3)
feat_t3 = eng.get_feature_names(tier=3)
assert len(feat_t3) >= 8, f'Tier 3 should have >=8 features, got {len(feat_t3)}'
assert any('rolling' in f for f in feat_t3), 'Missing rolling features'

print(f'Tier 1: {feat_t1}')
print(f'Tier 2: {feat_t2}')
print(f'Tier 3: {feat_t3}')
print('Feature engineering verified: progressive tiers work correctly')
"</automated>
  </verify>
  <done>
    - `FeatureEngineer.generate_all_features(tier=1)` returns DataFrame with 4 calendar feature columns + timestamp + load_mw
    - `FeatureEngineer.generate_all_features(tier=2)` adds lag_24h and lag_168h columns (6+ total features)
    - `FeatureEngineer.generate_all_features(tier=3)` adds rolling_mean_24h and rolling_std_24h (8+ total features)
    - All feature DataFrames are unscaled (no StandardScaler applied inside FeatureEngineer)
    - `get_feature_names()` returns correct ordered list per tier
    - Notebook 03 loads data, generates all three tiers progressively, and displays feature count comparison table
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: XGBoost forecaster enhancement + Notebook 04 (pipeline/forecaster.py)</name>
  <files>pipeline/forecaster.py, notebooks/04_load_forecasting.ipynb</files>
  <read_first>
    @.planning/phases/01-data-foundation-basic-prediction/01-CONTEXT.md §D-09 (TimeSeriesSplit, scaler fit-on-train), §D-14 (MAE only), §D-15 (plotly)
    @.planning/phases/01-data-foundation-basic-prediction/01-RESEARCH.md §3 (XGBoost config, TimeSeriesSplit, scaler pattern) and §4 (plotly patterns)
    @.planning/research/PITFALLS.md §Pitfall 1 (look-ahead bias — MUST enforce) and §Pitfall 5 (end-to-end early — already proven by Plan 01-01)
    @pipeline/forecaster.py (from Plan 01-01 SUMMARY — append to existing file, do NOT overwrite persistence_forecast/calculate_pnl/plot_pnl)
    @pipeline/features.py (from Task 1 of this plan — understand FeatureEngineer interface)
  </read_first>
  <action>
    **APPEND to `pipeline/forecaster.py`** (preserve all existing functions from Plan 01-01: persistence_forecast, calculate_pnl, plot_pnl). Add a new section with `# XGBoost Forecasting (Plan 01-02)` comment separator.

    Add new imports at top of file if not present: `import joblib`, `import numpy as np`, `from sklearn.model_selection import TimeSeriesSplit`, `from sklearn.preprocessing import StandardScaler`, `from xgboost import XGBRegressor`, `from pipeline.features import FeatureEngineer`, `import plotly.graph_objects as go`, `from plotly.subplots import make_subplots`.

    ---

    **Class: XGBoostForecaster** — per D-09, D-14, RESEARCH.md §3:

    - `__init__(self, feature_engineer: FeatureEngineer = None, **xgb_params)`:
      - `self.feature_engineer = feature_engineer` or `FeatureEngineer()` if None.
      - Default XGBRegressor params per RESEARCH.md §3: `n_estimators=100, max_depth=6, learning_rate=0.1, subsample=0.8, colsample_bytree=0.8, random_state=42`.
      - Allow override via `**xgb_params` passed to XGBRegressor.
      - `self.model = None` (trained model, set after train_evaluate).

    - `train_evaluate(X_raw: pd.DataFrame, y_col: str = 'load_mw', feature_tier: int = 1, n_splits: int = 5, gap: int = 24) -> dict`:
      a. Generate features: `X = self.feature_engineer.generate_all_features(X_raw, tier=feature_tier)`.
      b. Extract feature columns (exclude timestamp and load_mw): `feature_cols = self.feature_engineer.get_feature_names(feature_tier)`.
      c. Extract target: `y = X[y_col]`.
      d. Create `TimeSeriesSplit(n_splits=n_splits, gap=gap)`. Default: gap=24 per RESEARCH.md (prevents autocorrelation leakage).
      e. Initialize tracking: `fold_mae = []`, `all_preds = []`, `all_actuals = []`.
      f. For each fold `(train_idx, test_idx)` in `tscv.split(X)`:
         - `X_train_raw = X.iloc[train_idx]`, `X_test_raw = X.iloc[test_idx]`
         - `y_train = y.iloc[train_idx]`, `y_test = y.iloc[test_idx]`
         - CRITICAL (D-09 enforcement): Create a NEW `StandardScaler()` instance per fold: `scaler = StandardScaler()`. Fit ONLY on X_train features: `X_train_scaled = scaler.fit_transform(X_train_raw[feature_cols])`. Transform X_test: `X_test_scaled = scaler.transform(X_test_raw[feature_cols])`.
         - Create NEW `XGBRegressor(**self._xgb_params)` per fold: `model = XGBRegressor(**self._xgb_params)`.
         - `model.fit(X_train_scaled, y_train)`
         - `y_pred = model.predict(X_test_scaled)`
         - `mae = np.mean(np.abs(y_test.values - y_pred))`
         - Track: `fold_mae.append(mae)`, `all_preds.extend(y_pred.tolist())`, `all_actuals.extend(y_test.values.tolist())`.
      g. After loop: `self.model = XGBRegressor(**self._xgb_params)` fit on ALL training features (final model for prediction, not evaluation).
      h. Return dict: `{fold_mae: list[float], mean_mae: float, all_predictions: np.ndarray, all_actuals: np.ndarray, feature_names: list[str], n_folds: int}`.

    - `predict(X_raw: pd.DataFrame, feature_tier: int = 1) -> np.ndarray`:
      - Generate features via `self.feature_engineer.generate_all_features(X_raw, tier=feature_tier)`.
      - If `self.model` is None, raise RuntimeError("Model not trained. Call train_evaluate() first.").
      - Return `self.model.predict(X_features[feature_cols])`.

    - `save_model(self, path: str)`:
      - Serialize `self.model` to `path` using `joblib.dump(self.model, path)`.
      - Log saved path via `logging.info(...)`.

    - `load_model(self, path: str) -> XGBRegressor`:
      - `self.model = joblib.load(path)`. Return self.model.

    - `plot_forecast(y_true: pd.Series, y_pred: np.ndarray, timestamps: pd.DatetimeIndex = None) -> go.Figure`:
      - Use `make_subplots(rows=2, cols=1, shared_xaxes=True, vertical_spacing=0.08, subplot_titles=('Load Forecast vs Actual', 'Prediction Error Distribution'))`.
      - Subplot 1 (top): `go.Scatter` for actual (line=blue, name='Actual Load') and predicted (line=orange, name='XGBoost Prediction'). Use timestamps for x-axis if provided, else range index.
      - Subplot 2 (bottom): `go.Histogram` of errors (`y_true - y_pred`), nbinsx=30, marker_color='lightblue'. Add vertical line at x=0: `fig.add_vline(x=0, line_dash='dash', line_color='red', row=2, col=1)`.
      - Layout: title='XGBoost Load Forecast Evaluation', xaxis2_title='Time', yaxis_title='Load (MW)', yaxis2_title='Error Count'.
      - Set `pio.renderers.default = 'notebook'` for Jupyter rendering.

    **DO NOT modify existing functions** (persistence_forecast, calculate_pnl, plot_pnl) — they remain unchanged from Plan 01-01.

    ---

    **notebooks/04_load_forecasting.ipynb** — trains XGBoost per D-09, D-14, D-15:

    Structure:
    1. Markdown: `# 04 — 负荷预测：XGBoost + TimeSeriesSplit (Load Forecasting)` with learning objectives: train an XGBoost model, understand temporal cross-validation, evaluate with MAE, visualize predictions.
    2. Code: `from pipeline.data_loader import OWIDChinaLoader; from pipeline.cleaner import clean_data; from pipeline.features import FeatureEngineer; from pipeline.forecaster import XGBoostForecaster; import numpy as np; import plotly.io as pio; pio.renderers.default = 'notebook'`
    3. Markdown: `### Step 1: Load and Clean Data`
    4. Code: `loader = OWIDChinaLoader(); df = loader.load_data(); df_clean = clean_data(df); print(f"Data: {len(df_clean)} rows, {df_clean['timestamp'].min()} to {df_clean['timestamp'].max()}"); df_clean.head()`
    5. Markdown: `### Step 2: Configure Forecaster with Tier 1 Features`
    6. Code: `engineer = FeatureEngineer(); forecaster = XGBoostForecaster(feature_engineer=engineer); print(f"Features: {forecaster.feature_engineer.get_feature_names(tier=1)}")`
    7. Markdown: `### Step 3: Train with TimeSeriesSplit (5 folds, gap=24)`
    8. Code: `results = forecaster.train_evaluate(df_clean, feature_tier=1, n_splits=5, gap=24)` — note: for yearly OWID data, adapt `gap=1` since there's no hourly autocorrelation concern. Add a code comment: "For yearly data, gap=1 is sufficient. For hourly data, use gap=24 to prevent 24h autocorrelation leakage (per PITFALLS.md)."
    9. Code: `print(f"Per-fold MAE: {[round(m, 2) for m in results['fold_mae']]}"); print(f"Mean MAE: {results['mean_mae']:.2f} MW")` — per D-14, MAE is the ONLY reported metric. Do NOT compute or display RMSE/MAPE.
    10. Markdown: `### Step 4: Visualize Predictions`
    11. Code: `fig = forecaster.plot_forecast(y_true=pd.Series(results['all_actuals']), y_pred=results['all_predictions']); fig.show()` — renders interactive actual-vs-predicted overlay + error histogram per D-15.
    12. Markdown: `### Step 5: Save Model`
    13. Code: `forecaster.save_model('models/xgboost_tier1.joblib'); print("Model saved to models/xgboost_tier1.joblib")`
    14. Markdown: `### Step 6 (Optional): Try Tier 2/3 Features`
    15. Code: `forecaster2 = XGBoostForecaster(feature_engineer=FeatureEngineer()); results2 = forecaster2.train_evaluate(df_clean, feature_tier=3, n_splits=5, gap=1); print(f"Tier 3 Mean MAE: {results2['mean_mae']:.2f} MW")`
    16. Markdown: `### 思考题 (Reflection)` with questions: (a) Why does MAE vary across folds? What does this tell you about temporal data? (b) What does the `gap=24` parameter prevent? (Hint: think about lag-24h feature.) (c) Why do we use MAE instead of RMSE for this learning project? (d) What would you need to change to make this model work with hourly data instead of yearly data?
  </action>
  <verify>
    <automated>cd /mnt/e/Ellectric && source .venv/bin/activate && python -c "
from pipeline.data_loader import OWIDChinaLoader
from pipeline.cleaner import clean_data
from pipeline.features import FeatureEngineer
from pipeline.forecaster import XGBoostForecaster

# Load and clean
loader = OWIDChinaLoader()
df = loader.load_data()
df_clean = clean_data(df)

# Train XGBoost with Tier 1
engineer = FeatureEngineer()
forecaster = XGBoostForecaster(feature_engineer=engineer)
results = forecaster.train_evaluate(df_clean, feature_tier=1, n_splits=5, gap=1)

# Verify results structure
assert 'fold_mae' in results, 'Missing fold_mae'
assert 'mean_mae' in results, 'Missing mean_mae'
assert 'all_predictions' in results, 'Missing all_predictions'
assert 'all_actuals' in results, 'Missing all_actuals'
assert len(results['fold_mae']) == 5, f'Expected 5 folds, got {len(results[\"fold_mae\"])}'
assert results['mean_mae'] > 0, 'MAE should be positive'

print(f'Per-fold MAE: {results[\"fold_mae\"]}')
print(f'Mean MAE: {results[\"mean_mae\"]:.2f}')

# Verify plot functions
fig = forecaster.plot_forecast(
    y_true=pd.Series(results['all_actuals']),
    y_pred=results['all_predictions']
)
assert hasattr(fig, 'show'), 'Not a plotly figure'

# Verify save/load
import os, tempfile, joblib
forecaster.save_model('/tmp/test_xgb_model.joblib')
assert os.path.exists('/tmp/test_xgb_model.joblib'), 'Model not saved'
loaded_model = joblib.load('/tmp/test_xgb_model.joblib')
assert loaded_model is not None, 'Model not loadable'

# Verify predict
preds = forecaster.predict(df_clean, feature_tier=1)
assert len(preds) == len(df_clean), f'Predict length {len(preds)} != data length {len(df_clean)}'

print('XGBoost forecasting verified: train_evaluate, save/load, predict, plot_forecast all work')
" 2>&1</automated>
  </verify>
  <done>
    - `XGBoostForecaster.train_evaluate()` runs 5-fold TimeSeriesSplit with `gap=1` (yearly data) and returns per-fold MAE + mean MAE
    - Each fold creates a NEW StandardScaler and fits ONLY on X_train (per D-09 enforcement)
    - `results['mean_mae']` is a positive float in MW units
    - `XGBoostForecaster.save_model()` serializes trained model via joblib
    - `XGBoostForecaster.load_model()` deserializes from joblib
    - `XGBoostForecaster.predict()` returns np.ndarray of predictions matching input length
    - `XGBoostForecaster.plot_forecast()` returns a plotly Figure with actual-vs-predicted overlay + error histogram
    - Notebook 04 loads data, trains XGBoost, displays MAE per fold, renders interactive plotly charts, and saves model
    - MAE is the ONLY metric displayed (per D-14); no RMSE, no MAPE
    - Existing functions (persistence_forecast, calculate_pnl, plot_pnl) from Plan 01-01 remain intact and functional
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: DataLoader full implementation + Cleaner enhancement + Notebook 02 (pipeline/data_loader.py, pipeline/cleaner.py)</name>
  <files>pipeline/data_loader.py, pipeline/cleaner.py, notebooks/02_data_cleaning.ipynb, pipeline/__init__.py</files>
  <read_first>
    @.planning/phases/01-data-foundation-basic-prediction/01-CONTEXT.md §D-01 (China data), §D-03 (manual download), §D-04 (Parquet), §D-05 (standardized schema)
    @.planning/phases/01-data-foundation-basic-prediction/01-RESEARCH.md §1 (data strategy), §5 (DataLoader full interface: load_hourly_demand, metadata, versioning)
    @pipeline/data_loader.py (from Plan 01-01 SUMMARY — enhance, do NOT rewrite OWIDChinaLoader)
    @pipeline/cleaner.py (from Plan 01-01 SUMMARY — enhance)
  </read_first>
  <action>
    **ENHANCE existing files** (append new methods/features; do NOT remove or rewrite existing working code from Plan 01-01):

    ---

    **pipeline/data_loader.py** — enhance ChineseDataLoader per RESEARCH.md §5, add data version tracking:

    Add to **DataLoader ABC** (if not already present):
    - `data_version: str` property (abstract, return "unknown")
    - `get_metadata() -> dict` returning `{source, region, frequency, timerange, missing_pct, data_version}`

    Enhance **OWIDChinaLoader**:
    - Override `get_metadata()`: compute frequency from timestamp spacing (most common diff), compute missing_pct from NaN count in load_mw, return full dict.
    - Set `data_version` to `f"owid-{datetime.date.today().isoformat()}"` (date of fetch).

    Enhance **ChineseDataLoader**:
    - Add `load_hourly_demand(self, start=None, end=None) -> pd.DataFrame` method: alias for `load_data()` but validates that timestamp frequency is hourly (or sub-hourly). If yearly/daily data, logs warning that data is not hourly.
    - Add `get_metadata()`: reads file mtime, counts rows/cols, computes missing_pct, detects frequency by most common timestamp diff.
    - Add `data_version` property: returns `os.path.getmtime(self.data_path)` as ISO string if file exists, else `"not_available"`.
    - Add data quality checks in `load_data()`:
      - After loading, check for duplicate timestamps -> log warning via `logging.warning(f"Found {n} duplicate timestamps")`, keep first.
      - Check for time gaps larger than expected frequency -> log warning.
      - If `load_mw` column is ALL NaN after loading -> log error and return empty DataFrame.
    - `load_data()` should NOT raise exception if file missing — return empty DataFrame with correct schema + WARNING log with instructions (already implemented in Plan 01-01; verify and enhance if needed).

    ---

    **pipeline/cleaner.py** — enhance with additional validation per DATA-03:

    Add new helper functions (append, do NOT modify existing `clean_data`, `validate_schema`, `get_cleaning_report`):

    - `detect_timezone(df: pd.DataFrame) -> str`: returns the timezone string of the timestamp column (e.g., "UTC", "Asia/Shanghai", "timezone-naive"). Logs result.

    - `standardize_frequency(df: pd.DataFrame, target_freq: str = 'h') -> pd.DataFrame`:
      - Detect current frequency via `pd.infer_freq(df['timestamp'])`.
      - If None (irregular), resample to `target_freq` (e.g., 'h' for hourly) using mean aggregation for load_mw. Forward-fill metadata columns.
      - Log the detected and target frequencies.
      - Return resampled DataFrame or original if frequency matches.

    - `get_data_quality_score(df: pd.DataFrame) -> dict`:
      - Returns dict with: `{completeness: float (0-1, fraction non-NaN in load_mw), uniqueness: float (0-1, fraction unique timestamps), regularity: str (detected frequency or "irregular"), outlier_count: int}`
      - Used for data quality reporting in notebooks.

    ---

    **notebooks/02_data_cleaning.ipynb** — full cleaning pipeline demo per D-03:

    Structure:
    1. Markdown: `# 02 — 数据清洗 (Data Cleaning)` with learning objectives: understand data quality concepts, apply cleaning transformations, detect and report outliers.
    2. Code: `from pipeline.data_loader import OWIDChinaLoader, ChineseDataLoader; from pipeline.cleaner import clean_data, validate_schema, get_cleaning_report, detect_timezone, get_data_quality_score; import plotly.express as px`
    3. Markdown: `### Step 1: Load Raw Data`
    4. Code: `loader = OWIDChinaLoader(); df_raw = loader.load_data(); print(f"Raw: {len(df_raw)} rows"); print(f"Metadata: {loader.get_metadata()}"); df_raw.head()`
    5. Code: `print(f"Timezone: {detect_timezone(df_raw)}")` — show timezone detection.
    6. Markdown: `### Step 2: Data Quality Assessment (Before Cleaning)`
    7. Code: `quality_before = get_data_quality_score(df_raw); pd.DataFrame([quality_before]).T.rename(columns={0: 'Before Cleaning'})` — display completeness, uniqueness, outlier count.
    8. Markdown: `### Step 3: Clean Data`
    9. Code: `df_clean = clean_data(df_raw)` — runs the cleaning pipeline.
    10. Code: `report = get_cleaning_report(df_raw, df_clean); print(report)` — shows what changed.
    11. Markdown: `### Step 4: Data Quality Assessment (After Cleaning)`
    12. Code: `quality_after = get_data_quality_score(df_clean); comparison = pd.DataFrame({'Before': quality_before, 'After': quality_after}).T; comparison` — side-by-side comparison.
    13. Code: `assert validate_schema(df_clean), "Schema validation failed!"; print("Schema validated: all columns correct, timezone-aware UTC")`
    14. Markdown: `### Step 5: Outlier Visualization`
    15. Code: `px.box(df_clean, y='load_mw', title='Load Distribution (MW) — Outliers Are The Signal, Not Noise')` — boxplot showing IQR range and outliers.
    16. Markdown: `### Step 6: Chinese Hourly Data (If Available)`
    17. Code: `try: cn_loader = ChineseDataLoader(data_path='data/your_hourly_data.csv'); df_cn = cn_loader.load_data(); print(df_cn.head())` wrapped in try/except with note: "Place your manually downloaded Chinese hourly/daily data files in the 'data/' directory. See docs/chinese-electricity-data-guide.md for download instructions."
    18. Markdown: `### 思考题 (Reflection)` with questions: (a) Why do we report outliers instead of removing them in electricity data? (b) What happens to predictions if you train a model on data with missing timestamps? (c) How would you handle timezone differences if you combined data from multiple Chinese provinces?
  </action>
  <verify>
    <automated>cd /mnt/e/Ellectric && source .venv/bin/activate && python -c "
from pipeline.data_loader import OWIDChinaLoader, ChineseDataLoader
from pipeline.cleaner import clean_data, validate_schema, get_cleaning_report, detect_timezone, get_data_quality_score

# Test enhanced DataLoader
loader = OWIDChinaLoader()
df = loader.load_data()
meta = loader.get_metadata()
assert 'source' in meta, 'Missing source in metadata'
assert 'frequency' in meta, 'Missing frequency in metadata'
assert 'missing_pct' in meta, 'Missing missing_pct in metadata'
assert loader.data_version is not None, 'Missing data_version'
print(f'Data version: {loader.data_version}')
print(f'Metadata: {meta}')

# Test cleaner enhancements
df_clean = clean_data(df)
tz = detect_timezone(df_clean)
assert 'UTC' in tz.upper(), f'Timezone should be UTC, got {tz}'
quality = get_data_quality_score(df_clean)
assert 'completeness' in quality, 'Missing completeness'
assert 0 <= quality['completeness'] <= 1, f'Completeness out of range: {quality[\"completeness\"]}'
print(f'Data quality: {quality}')

# Test ChineseDataLoader with non-existent file (should return empty, not crash)
cn = ChineseDataLoader(data_path='data/nonexistent.csv')
df_cn = cn.load_data()
assert len(df_cn) == 0, 'Should return empty DataFrame for missing file'
assert 'timestamp' in df_cn.columns, 'Empty DataFrame should have correct schema columns'

print('DataLoader + Cleaner enhancements verified')
"</automated>
  </verify>
  <done>
    - `OWIDChinaLoader.get_metadata()` returns dict with source, region, frequency, timerange, missing_pct, data_version
    - `ChineseDataLoader.load_data()` handles missing files gracefully (empty DataFrame, no exception)
    - `detect_timezone()` returns timezone string for timestamp column
    - `get_data_quality_score()` returns completeness, uniqueness, regularity, outlier_count
    - `validate_schema()` confirms UTC timezone-aware timestamps and float64 load_mw
    - Notebook 02 loads data, displays quality before/after, runs cleaning, shows outlier boxplot, and handles missing Chinese file gracefully
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
Same as Plan 01-01: local-only Jupyter environment, no network services. Enhanced model persistence adds a file I/O boundary.

| Boundary | Description |
|----------|-------------|
| PyPI → local venv | Package installs |
| Local filesystem | Model save/load via joblib |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Tampering | Model file via joblib | accept | Model files stored locally, no external access. joblib deserialization can execute arbitrary code if file is tampered — accepted for local-only learning platform (user controls all files). |
| T-01-02-02 | Information Disclosure | None | accept | Model contains only trained weights on public data — no PII or secrets. |
| T-01-02-03 | Spoofing | OWID data version tracking | accept | data_version uses fetch date; OWID data is HTTPS from GitHub. Accepted for learning purposes. |
</threat_model>

<verification>
Run comprehensive verification after Task 3:

```bash
cd /mnt/e/Ellectric && source .venv/bin/activate && python -c "
from pipeline.data_loader import OWIDChinaLoader, ChineseDataLoader
from pipeline.cleaner import clean_data, validate_schema, get_cleaning_report, get_data_quality_score
from pipeline.features import FeatureEngineer
from pipeline.forecaster import XGBoostForecaster, persistence_forecast, calculate_pnl, plot_pnl

print('=== FULL PIPELINE VERIFICATION ===')

# Data layer
loader = OWIDChinaLoader()
df = loader.load_data()
print(f'[DATA] {len(df)} rows, version={loader.data_version}, meta={loader.get_metadata()}')

# Cleaning
df_clean = clean_data(df)
assert validate_schema(df_clean)
print(f'[CLEAN] Schema OK, quality={get_data_quality_score(df_clean)}')

# Backward compat: persistence forecast still works
forecast = persistence_forecast(df_clean, shift_hours=1)
pnl = calculate_pnl(df_clean['load_mw'], forecast)
print(f'[PERSISTENCE] Final PnL: {pnl[\"cumulative_pnl\"].iloc[-1]:.2f}')

# Feature engineering
eng = FeatureEngineer()
df_feat = eng.generate_all_features(df_clean, tier=3)
print(f'[FEATURES] Tier 3: {len(eng.get_feature_names(3))} features')

# XGBoost
ft = XGBoostForecaster(feature_engineer=eng)
results = ft.train_evaluate(df_clean, feature_tier=1, n_splits=5, gap=1)
print(f'[XGB] Mean MAE: {results[\"mean_mae\"]:.2f} MW, per-fold: {[round(m,2) for m in results[\"fold_mae\"]]}')

# Model persistence
import tempfile, os
tmp = '/tmp/test_xgb_model.joblib'
ft.save_model(tmp)
assert os.path.exists(tmp)
ft2 = XGBoostForecaster(feature_engineer=eng)
ft2.load_model(tmp)
preds = ft2.predict(df_clean, feature_tier=1)
assert len(preds) == len(df_clean)
print(f'[MODEL] Saved/loaded/reloaded OK, predictions: {len(preds)}')

print('ALL CHECKS PASSED — Plan 01-02 complete')
"
```
</verification>

<success_criteria>
- [ ] FeatureEngineer generates 3 tiers of features: Tier 1 (4 calendar), Tier 2 (+2 lag), Tier 3 (+2 rolling) = 8+ total
- [ ] All features are unscaled; no StandardScaler.fit() inside FeatureEngineer
- [ ] XGBoostForecaster.train_evaluate() uses TimeSeriesSplit(n_splits=5, gap=1) with NEW StandardScaler per fold
- [ ] StandardScaler.fit_transform() called ONLY on X_train (per D-09 — no look-ahead bias)
- [ ] Per-fold MAE displayed; mean_mae computed; MAE is the ONLY metric (per D-14)
- [ ] plot_forecast() returns plotly Figure with actual-vs-predicted overlay + error histogram (per D-15)
- [ ] Model save/load via joblib works; predict() returns correct-length predictions
- [ ] ChineseDataLoader.get_metadata() returns complete metadata dict; handles missing files gracefully
- [ ] Cleaner enhancements (detect_timezone, get_data_quality_score) work correctly
- [ ] Notebooks 02/03/04 are self-contained: import pipeline modules, run with clear markdown → code → output structure
- [ ] All existing functions from Plan 01-01 (persistence_forecast, calculate_pnl, plot_pnl) remain unchanged and functional
</success_criteria>

<output>
Create `.planning/phases/01-data-foundation-basic-prediction/01-02-SUMMARY.md` when done
</output>
