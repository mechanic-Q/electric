---
phase: 01-data-foundation-basic-prediction
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - docker-compose.yml
  - README.md
  - docs/chinese-electricity-data-guide.md
  - notebooks/01_data_ingestion.ipynb
  - notebooks/02_data_cleaning.ipynb
  - notebooks/03_feature_engineering.ipynb
  - notebooks/04_load_forecasting.ipynb
  - notebooks/05_end_to_end_baseline.ipynb
autonomous: true
requirements: [ENV-02, ENV-03]

must_haves:
  truths:
    - "Learner can find docker-compose.yml with commented-out TimescaleDB + Grafana services and a comment explaining Phase 2 usage"
    - "Learner can read the data dictionary in README.md listing all columns, types, descriptions, and example values"
    - "Learner can follow docs/chinese-electricity-data-guide.md to manually download Chinese electricity data from and鲸/天池/地方平台"
    - "Every notebook has reflection questions (思考题) in markdown cells after the main content"
    - "Every notebook has error handling for missing data with clear user-facing messages"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Commented skeleton YAML for Phase 2 TimescaleDB + Grafana services"
      contains: "timescaledb"
    - path: "README.md"
      provides: "Complete project overview + data dictionary with all column schemas + setup instructions"
      min_lines: 60
      contains: "Data Dictionary"
    - path: "docs/chinese-electricity-data-guide.md"
      provides: "Step-by-step manual download guide for Chinese electricity data platforms"
      contains: "和鲸"
  key_links:
    - from: "docker-compose.yml"
      to: "Phase 2 ASSUME visualization"
      via: "comments explaining TimescaleDB + Grafana purpose"
    - from: "README.md Data Dictionary"
      to: "pipeline/data_loader.py OUTPUT_SCHEMA"
      via: "column names match DataLoader output"
    - from: "docs/chinese-electricity-data-guide.md"
      to: "notebooks/01_data_ingestion.ipynb §1.2"
      via: "referenced in notebook as source of download instructions"
---

## Phase Goal

**As a** learner, **I want to** find project documentation (README data dictionary, Chinese data acquisition guide), see polished Jupyter notebooks with reflection questions and error handling, and locate the Docker Compose skeleton for Phase 2, **so that** I have a complete, self-documenting learning platform that I can navigate independently.

<objective>
Polish the Phase 1 deliverable to a learning-ready state. Create the Docker Compose skeleton YAML (commented-out TimescaleDB + Grafana for Phase 2 ASSUME visualization — per D-07). Enhance the README with a full data dictionary. Write the Chinese electricity data acquisition guide (docs/chinese-electricity-data-guide.md) with manual download instructions. Add reflection questions and error handling to all 5 Jupyter notebooks. This is the final "wrap-up" plan — all core functionality was built in Plans 01-01 and 01-02.

Purpose: Transform working code into a self-contained learning platform. A new learner should be able to read README.md, follow the setup, download data per the guide, and work through all 5 notebooks with embedded reflection questions.

Output: 8 files (1 new YAML, 1 new docs markdown, 5 notebook enhancements, 1 README enhancement).
</objective>

<execution_context>
@/mnt/e/Ellectric/.opencode/get-shit-done/workflows/execute-plan.md
@/mnt/e/Ellectric/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@.planning/phases/01-data-foundation-basic-prediction/01-CONTEXT.md

## Prior Plan Output (from 01-01 and 01-02)
@.planning/phases/01-data-foundation-basic-prediction/01-01-SUMMARY.md
@.planning/phases/01-data-foundation-basic-prediction/01-02-SUMMARY.md
> NOTE: load both SUMMARYs for exact file contents, column names, function signatures, and module structure. Notebooks 01-05 already exist; this plan enhances them.

## Key Decisions
- **D-07**: Docker Compose optional — NOT required for Phase 1. Create skeleton YAML only with commented-out services, marked as "Phase 2 dependency."
- **D-03**: Manual download workflow for Chinese data — document the steps clearly
- **D-11**: Notebook naming convention already implemented (01_data_ingestion through 05_end_to_end_baseline)
- **D-15**: plotly is the visualization library — ensure all notebook visualizations use plotly

## Context from RESEARCH.md
- **Chinese data platforms** (RESEARCH.md §1): 和鲸社区, 阿里天池, 地方开放数据平台 (深圳/上海/广东/菏泽) — all require manual browser access
- **ENV-02**: Docker Compose for TimescaleDB + Grafana (ASSUME visualization dependency in Phase 2)
- **ENV-03**: Jupyter notebooks with markdown → code → visualization → reflection structure
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Docker Compose skeleton (docker-compose.yml)</name>
  <files>docker-compose.yml</files>
  <read_first>
    @.planning/phases/01-data-foundation-basic-prediction/01-CONTEXT.md §D-07 (skeleton only, marked as Phase 2 dependency)
    @.planning/REQUIREMENTS.md §ENV-02 (Docker Compose for TimescaleDB + Grafana)
    @.planning/research/STACK.md §Supporting Infrastructure (Docker, TimescaleDB, Grafana references)
  </read_first>
  <action>
    Create `docker-compose.yml` at project root per D-07 and ENV-02. All services are COMMENTED OUT — this is a skeleton for Phase 2 activation.

    File structure (YAML with extensive YAML comments using `#`):

    ```yaml
    # =============================================================================
    # Ellectric — Docker Compose Skeleton
    # =============================================================================
    # These services are NOT required for Phase 1.
    # Uncomment and run in Phase 2 when ASSUME market simulation is introduced.
    # ASSUME uses TimescaleDB for data storage and Grafana for dashboards.
    #
    # Usage in Phase 2:
    #   docker compose up -d
    #   docker compose down
    # =============================================================================
    ```

    Service definitions (ALL commented out with `# ` prefix on every line):

    **timescaledb service** (commented):
    - image: `timescale/timescaledb:latest-pg16`
    - container_name: `ellectric-timescaledb`
    - ports: `"5432:5432"`
    - environment: `POSTGRES_USER=ellectric`, `POSTGRES_PASSWORD=ellectric`, `POSTGRES_DB=assume`
    - volumes: `timescaledb_data:/var/lib/postgresql/data`
    - healthcheck: `pg_isready -U ellectric -d assume`
    - restart: `unless-stopped`

    **grafana service** (commented):
    - image: `grafana/grafana:latest`
    - container_name: `ellectric-grafana`
    - ports: `"3000:3000"`
    - environment: `GF_SECURITY_ADMIN_USER=admin`, `GF_SECURITY_ADMIN_PASSWORD=admin`
    - depends_on: `timescaledb`
    - volumes: `grafana_data:/var/lib/grafana`
    - restart: `unless-stopped`

    **volumes** (commented):
    - `timescaledb_data:`
    - `grafana_data:`

    Above the commented services, add a prominent uncommented header block:
    ```
    # All services below are commented out — this is a skeleton for Phase 2.
    # Uncomment the services you need and run: docker compose up -d
    # Phase 1 requires NO Docker services — everything runs locally via Jupyter.
    ```
  </action>
  <verify>
    <automated>bash -c 'test -f docker-compose.yml && echo "File exists" && grep -c "timescaledb" docker-compose.yml && grep -c "grafana" docker-compose.yml && echo "Both services present"'</automated>
    <automated>python3 -c "
with open('docker-compose.yml') as f:
    content = f.read()
    assert 'timescale/timescaledb' in content, 'Missing TimescaleDB image'
    assert 'grafana/grafana' in content, 'Missing Grafana image'
    assert 'Phase 2' in content, 'Missing Phase 2 reference'
    assert 'Phase 1' in content and 'NOT required' in content, 'Missing Phase 1 note'
    assert '5432:5432' in content, 'Missing timescaledb port'
    assert '3000:3000' in content, 'Missing grafana port'
    # Verify services are commented (lines starting with # after the header block)
    lines = content.split('\n')
    service_lines = [l for l in lines if 'timescaledb:' in l or 'grafana:' in l or 'volumes:' in l]
    for line in service_lines:
        stripped = line.strip()
        if stripped.startswith('timescaledb:') or stripped.startswith('grafana:') or stripped.startswith('volumes:'):
            assert line.strip().startswith('#'), f'Expected commented line, got: {line[:80]}'
    print('Docker Compose skeleton verified')
"</automated>
  </verify>
  <done>
    - `docker-compose.yml` exists at project root with timescaledb and grafana service definitions
    - Both services and their volumes are commented out with `#` prefix
    - File includes header explaining this is a Phase 2 skeleton (Phase 1 requires no Docker)
    - TimescaleDB configured with ports 5432, Grafana with port 3000
    - All environment variables use `ellectric` / `admin` as defaults (learning platform — no security concern)
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: README data dictionary + Chinese data acquisition guide (README.md + docs/)</name>
  <files>README.md, docs/chinese-electricity-data-guide.md</files>
  <read_first>
    @.planning/phases/01-data-foundation-basic-prediction/01-CONTEXT.md §D-03 (manual download workflow doc), §D-05 (standardized column schema)
    @.planning/phases/01-data-foundation-basic-prediction/01-RESEARCH.md §1 (Chinese data platforms — and鲸, 天池, 地方平台, URLs, access results)
    @README.md (existing from Plan 01-01 — enhance, do NOT overwrite project title/quick start)
  </read_first>
  <action>
    **ENHANCE `README.md`** at project root (append new sections; preserve existing title, Phase 1 badge, description, and quick start section from Plan 01-01):

    Add section `## Data Dictionary` after the quick start section:

    ```markdown
    ## Data Dictionary

    ### Standardized Schema (All DataLoaders)

    | Column | Type | Description | Example Values |
    |--------|------|-------------|----------------|
    | `timestamp` | `datetime64[ns, UTC]` | UTC timestamp (timezone-aware) | `2023-01-01T00:00:00+00:00` |
    | `load_mw` | `float64` | Electricity load/demand in megawatts (MW) | `523000.5` |
    | `data_source` | `str` | Origin of the data | `OWID`, `chinese_platform` |
    | `region` | `str` | Geographic region | `China`, `Guangdong` |

    ### OWID China Yearly Data

    | Column | Description | Unit | Example |
    |--------|-------------|------|---------|
    | `year` | Calendar year | — | 2023 |
    | `electricity_generation` | Total electricity generation in China | TWh | 9,456.4 |
    | `electricity_demand` | Total electricity demand (mapped to `load_mw` in standardized schema) | TWh | 9,200.0 |
    | `coal_electricity` | Generation from coal | TWh | 5,200.0 |
    | `solar_electricity` | Generation from solar PV | TWh | 580.0 |
    | `wind_electricity` | Generation from wind | TWh | 880.0 |

    ### Chinese Manual Download Data

    | Column | Description | Unit | Required |
    |--------|-------------|------|----------|
    | `timestamp` | Time of measurement (local time, auto-converted to UTC) | datetime | ✅ |
    | `load_mw` | Electricity load | MW | ✅ |
    | `region` | Province or city (optional, defaults to `China`) | str | ❌ |

    > **Note:** The standardized schema output (after DataLoader) always uses `timestamp` (UTC) and `load_mw` (MW).
    > Input files from Chinese platforms may use different column names — ChineseDataLoader maps them automatically
    > (e.g., `时间` → `timestamp`, `负荷` → `load_mw`).
    ```

    Add section `## Project Structure`:

    Show the directory tree as it exists after Phase 1:
    ```
    ellectric/
    ├── pipeline/           # Reusable Python modules
    │   ├── __init__.py
    │   ├── data_loader.py  # DataLoader ABC + OWIDChinaLoader + ChineseDataLoader
    │   ├── cleaner.py      # Data cleaning pipeline
    │   ├── features.py     # Feature engineering (Tier 1-3)
    │   └── forecaster.py   # Persistence forecast + XGBoostForecaster
    ├── notebooks/          # Jupyter notebooks (sequential learning path)
    │   ├── 01_data_ingestion.ipynb
    │   ├── 02_data_cleaning.ipynb
    │   ├── 03_feature_engineering.ipynb
    │   ├── 04_load_forecasting.ipynb
    │   └── 05_end_to_end_baseline.ipynb
    ├── data/               # Raw data storage (gitignored)
    ├── docs/               # Documentation
    │   └── chinese-electricity-data-guide.md
    ├── models/             # Trained model persistence (gitignored)
    ├── setup.sh            # One-command environment setup
    ├── requirements.txt    # Pinned Python dependencies
    ├── docker-compose.yml  # Phase 2 skeleton (commented out)
    └── README.md
    ```

    Add section `## Learning Path`:
    - Brief description of the 5-notebook sequence
    - Suggested order: 01 → 02 → 03 → 04 → 05
    - Note that 05 (E2E baseline) can be run first to get a quick win, then go back to 01

    Add section `## Phase 2 Preview` referencing docker-compose.yml and what ASSUME/OpenSTEF will add.

    ---

    **CREATE `docs/chinese-electricity-data-guide.md`** per D-03:

    Content structure:

    1. **Title**: `# Chinese Electricity Data Acquisition Guide` with subtitle: "How to manually download Chinese electricity load data for the Ellectric learning platform."

    2. **Why Manual Download?** section: explain that Chinese government open data platforms (深圳/上海/广东/菏泽) require browser interaction (CAPTCHA, login, etc.) and cannot be programmatically accessed. OWID provides auto-fetchable yearly macro data; hourly/daily data requires manual effort.

    3. **Recommended Platforms** table:

    | Platform | URL | Data Type | Access Method | Notes |
    |----------|-----|-----------|---------------|-------|
    | 和鲸社区 (Heywhale) | https://www.heywhale.com/ | Hourly load, generation | Free registration → search "电力负荷" → download dataset | Most active Chinese data science community; search for "电力", "负荷预测" |
    | 阿里天池 (Tianchi) | https://tianchi.aliyun.com/ | Various | Free registration → competition datasets → search "电力" | Look for electricity forecasting competition datasets |
    | 深圳市政府数据开放平台 | https://opendata.sz.gov.cn/ | Regional load | Free registration + real-name verification | May have district-level load data |
    | 广东省公共数据开放平台 | https://data.gd.gov.cn/ | Regional load | Free registration | Provincial-level data |
    | 菏泽市公共数据开放网 | (search for latest URL) | Local load | Free registration | City-level data, exemplar of local open data |

    4. **Step-by-Step Download Instructions** (for 和鲸社区 as primary example):
       - Step 1: Visit https://www.heywhale.com/
       - Step 2: Register a free account (email or WeChat)
       - Step 3: Search for "电力负荷" (electricity load) or "电力数据" (electricity data)
       - Step 4: Browse results, look for datasets with timestamps and MW values
       - Step 5: Download the CSV or Excel file
       - Step 6: Place the file in the `ellectric/data/` directory

    5. **Expected File Format** section:
       - Minimum required columns: a timestamp column and a load value column (MW)
       - Accepted formats: CSV (`.csv`), Excel (`.xlsx`), Parquet (`.parquet`)
       - The `ChineseDataLoader` auto-detects format by file extension
       - If column names differ (e.g., Chinese characters like `时间`, `负荷`), rename them to `timestamp` and `load_mw` before use, OR ChineseDataLoader will attempt auto-mapping for common Chinese column names.

    6. **Verification** section:
       ```python
       from pipeline.data_loader import ChineseDataLoader
       loader = ChineseDataLoader(data_path='data/your_file.csv')
       df = loader.load_data()
       print(df.head())
       print(loader.get_metadata())
       ```

    7. **Fallback: OWID Yearly Data** section: if you cannot find Chinese hourly data, the `OWIDChinaLoader` provides auto-fetchable yearly China energy data — this is sufficient for learning the pipeline fundamentals.
  </action>
  <verify>
    <automated>bash -c 'test -f README.md && test -f docs/chinese-electricity-data-guide.md && echo "Both files exist"'</automated>
    <automated>python3 -c "
# Verify README sections
with open('README.md') as f:
    content = f.read()
    assert 'Data Dictionary' in content, 'Missing Data Dictionary section'
    assert 'Project Structure' in content, 'Missing Project Structure section'
    assert 'timestamp' in content and 'load_mw' in content, 'Missing standardized schema columns'
    assert 'OWID' in content, 'Missing OWID data source reference'
    assert 'setup.sh' in content, 'Missing setup.sh reference'
    print('README.md verified: Data Dictionary + Project Structure present')

# Verify Chinese data guide
with open('docs/chinese-electricity-data-guide.md') as f:
    guide = f.read()
    assert '和鲸' in guide, 'Missing Heywhale reference'
    assert '天池' in guide, 'Missing Tianchi reference'
    assert 'download' in guide.lower(), 'Missing download instructions'
    assert 'ChineseDataLoader' in guide, 'Missing DataLoader reference'
    assert '.csv' in guide or 'CSV' in guide, 'Missing CSV format mention'
    print('docs/chinese-electricity-data-guide.md verified')
"</automated>
  </verify>
  <done>
    - README.md has `## Data Dictionary` section with standardized schema (timestamp, load_mw, data_source, region) and OWID-specific columns
    - README.md has `## Project Structure` section with directory tree
    - README.md has `## Learning Path` with notebook sequence (01→05)
    - `docs/chinese-electricity-data-guide.md` lists 和鲸, 天池, 深圳, 广东, 菏泽 as data sources with URLs
    - Guide includes step-by-step download instructions for 和鲸社区
    - Guide explains expected file format (CSV/Excel/Parquet with timestamp + load_mw)
    - Guide includes verification code snippet using ChineseDataLoader
    - Guide mentions OWID yearly data as fallback
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Notebook polish — reflection questions + error handling across all 5 notebooks</name>
  <files>
    notebooks/01_data_ingestion.ipynb,
    notebooks/02_data_cleaning.ipynb,
    notebooks/03_feature_engineering.ipynb,
    notebooks/04_load_forecasting.ipynb,
    notebooks/05_end_to_end_baseline.ipynb
  </files>
  <read_first>
    @.planning/REQUIREMENTS.md §ENV-03 (notebook structure: markdown → code → visualization → reflection)
    @.planning/phases/01-data-foundation-basic-prediction/01-CONTEXT.md §D-10 (thin notebooks, thick modules), §D-11 (naming convention)
    @.planning/phases/01-data-foundation-basic-prediction/01-01-SUMMARY.md (for notebook content already created)
    @.planning/phases/01-data-foundation-basic-prediction/01-02-SUMMARY.md (for notebook content already created)
  </read_first>
  <action>
    **ENHANCE all 5 notebooks** per ENV-03 (markdown → code → visualization → 思考题) and D-10 (thin notebooks). READ each notebook first to understand its current structure, then add or enhance the following:

    ---

    **For ALL 5 notebooks:**

    1. **Title markdown cell enhancement**: add learning objectives as bullet points after the title. Format:
       ```
       # 0X — Title
       
       **学习目标 (Learning Objectives):**
       - Objective 1
       - Objective 2
       - Objective 3
       ```

    2. **Error handling for data loading cells**: wrap data-loading code in each notebook's first pipeline call with:
       ```python
       try:
           loader = OWIDChinaLoader()
           df = loader.load_data()
           print(f"✅ Data loaded: {len(df)} rows")
       except Exception as e:
           print(f"❌ Failed to load OWID data: {e}")
           print("Check your internet connection or try loading cached data from data/owid_energy_raw.csv")
           print("If you have Chinese hourly data, use ChineseDataLoader(data_path='data/your_file.csv') instead.")
       ```

    3. **Reflection section (思考题)**: ensure each notebook ENDS with a markdown cell titled `### 💭 思考题 (Reflection Questions)` containing 2-4 questions. Questions should:
       - Connect to real-world electricity trading scenarios
       - Probe understanding of pitfalls (look-ahead bias, spike-as-signal)
       - Relate to the Phase 1 learning objectives
       - Be answerable from what the learner just saw in the notebook outputs

    4. **Cell numbering**: number each major section's markdown cell (e.g., `### Step 1: xxx`, `### Step 2: xxx`) so the notebook reads like a guided tutorial.

    ---

    **Notebook-specific enhancements:**

    **01_data_ingestion.ipynb** — reflection questions to ADD (if not already present):
    1. OWID data is yearly — what limitations does this have for short-term load forecasting?
    2. Why is it harder to get Chinese hourly electricity data compared to US PUDL data?
    3. If you had hourly load data for Guangdong province, how would you structure the DataLoader?

    **02_data_cleaning.ipynb** — reflection questions to ADD:
    1. Why do we report outliers instead of removing them in electricity data? (Hint: think about peak summer loads)
    2. What would happen in a model trained on data that still has missing timestamps?
    3. If you saw a load value of 0 MW at noon on a weekday, would you trust it? What would you investigate?

    **03_feature_engineering.ipynb** — reflection questions to ADD:
    1. The `lag_168h` feature uses last week's load. Why is this a strong signal for load forecasting?
    2. We do NOT scale features inside `FeatureEngineer` — why must scaling happen inside the forecaster's training loop instead?
    3. If you had weather data (temperature, cloud cover), which additional features would you engineer?

    **04_load_forecasting.ipynb** — reflection questions to ADD:
    1. The MAE varies across the 5 folds — what does this tell you about temporal data non-stationarity?
    2. What does the `gap=24` parameter in TimeSeriesSplit prevent? (Think about the lag-24h feature.)
    3. How well does XGBoost perform vs the persistence baseline from Notebook 05? Which would you trust more?
    4. What would change if you trained this model on hourly data instead of yearly data?

    **05_end_to_end_baseline.ipynb** — reflection questions to ADD:
    1. A positive cumulative P&L means the strategy is profitable in this simplified simulation. What realistic costs are missing?
    2. The persistence forecast assumes "tomorrow = yesterday." When would this assumption completely fail?
    3. This notebook uses yearly OWID data (1 data point per year). How would a full year of hourly data (8,760 points) change the depth of analysis you can do?

    ---

    **Additional polish items** (apply where missing):

    - **Import consolidation**: ensure all imports are at the top of each notebook in one cell (PEP 8 style for notebooks)
    - **plotly rendering**: add `pio.renderers.default = 'notebook'` in an early cell if not already present
    - **Final cell in each notebook**: add a markdown cell with `---` separator and a "Next notebook: [0X_name]" link to the sequential notebook
    - **Data source attribution**: add a markdown cell after data loading noting the data source (OWID → Our World in Data, CC BY 4.0; Chinese platforms → see guide)
  </action>
  <verify>
    <automated>cd /mnt/e/Ellectric && source .venv/bin/activate && python -c "
import nbformat
import os

notebook_dir = 'notebooks'
expected_notebooks = [
    '01_data_ingestion.ipynb',
    '02_data_cleaning.ipynb',
    '03_feature_engineering.ipynb',
    '04_load_forecasting.ipynb',
    '05_end_to_end_baseline.ipynb'
]

for nb_name in expected_notebooks:
    nb_path = os.path.join(notebook_dir, nb_name)
    assert os.path.exists(nb_path), f'Missing notebook: {nb_path}'
    
    with open(nb_path) as f:
        nb = nbformat.read(f, as_version=4)
    
    # Convert to text for keyword search
    text = ' '.join(cell.source for cell in nb.cells)
    
    # Check for reflection section
    assert '思考题' in text, f'{nb_name}: Missing 思考题 section'
    assert 'Reflection' in text, f'{nb_name}: Missing Reflection section'
    
    # Check for error handling
    assert 'try' in text.lower() or 'except' in text.lower() or 'try:' in text, \
        f'{nb_name}: Missing try/except error handling'
    
    # Check for learning objectives
    assert '学习目标' in text or 'Learning Objectives' in text, \
        f'{nb_name}: Missing learning objectives'
    
    # Check for plotly renderer in relevant notebooks
    if nb_name in ['04_load_forecasting.ipynb', '05_end_to_end_baseline.ipynb']:
        assert 'pio.renderers' in text or 'plotly' in text.lower(), \
            f'{nb_name}: Missing plotly renderer setup'
    
    print(f'{nb_name}: ✅ {len(nb.cells)} cells — reflection + error handling + objectives present')

print('All 5 notebooks verified')
"</automated>
  </verify>
  <done>
    - All 5 notebooks have a `### 💭 思考题 (Reflection Questions)` markdown cell with 2-4 questions at the end
    - All 5 notebooks have learning objectives displayed after the title
    - All 5 notebooks have try/except error handling for data loading with user-friendly messages
    - Plotly notebooks (04, 05) have `pio.renderers.default = 'notebook'` configured
    - Each notebook imports are consolidated at the top
    - Each notebook has a "Next notebook" link at the end (except 05 which says "End of Phase 1")
    - Notebooks still run from top to bottom without errors (existing functionality preserved)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
No new trust boundaries introduced. Docker Compose file is commented-out and inert.

| Boundary | Description |
|----------|-------------|
| None new | All work is documentation and notebook enhancement |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Information Disclosure | ChineseDataLoader guide lists public URLs | accept | All URLs are publicly known government open data portals — no credential exposure risk. |
| T-01-03-02 | Tampering | docker-compose.yml | accept | File is for reference only. All services are commented out — no services are started. Accepted. |
</threat_model>

<verification>
Run notebook validation after Task 3:

```bash
cd /mnt/e/Ellectric && source .venv/bin/activate && python -c "
import nbformat, os

notebook_dir = 'notebooks'
expected = [
    '01_data_ingestion.ipynb',
    '02_data_cleaning.ipynb',
    '03_feature_engineering.ipynb',
    '04_load_forecasting.ipynb',
    '05_end_to_end_baseline.ipynb'
]

all_ok = True
for nb_name in expected:
    path = os.path.join(notebook_dir, nb_name)
    with open(path) as f:
        nb = nbformat.read(f, as_version=4)
    text = ' '.join(cell.source for cell in nb.cells)
    
    checks = {
        '学习目标': '学习目标' in text or 'Learning Objectives' in text,
        '思考题': '思考题' in text,
        'Error handling': 'try' in text or 'except' in text,
        'Not empty': len(nb.cells) >= 3
    }
    
    status = '✅' if all(checks.values()) else '❌'
    failed = [k for k, v in checks.items() if not v]
    print(f'{status} {nb_name}: {len(nb.cells)} cells', end='')
    if failed:
        print(f' — MISSING: {failed}')
        all_ok = False
    else:
        print()

if all_ok:
    print('All notebooks pass validation')
else:
    print('Some notebooks have missing elements — see above')
"

# Additional checks:
echo "---" && echo "File existence check:" && ls -la docker-compose.yml docs/chinese-electricity-data-guide.md README.md && echo "All Plan 01-03 files exist"
```
</verification>

<success_criteria>
- [ ] `docker-compose.yml` exists with commented-out timescaledb + grafana services, marked as "Phase 2 skeleton"
- [ ] `README.md` has Data Dictionary section with standardized schema (timestamp, load_mw, data_source, region types and examples)
- [ ] `README.md` has Project Structure section showing directory tree
- [ ] `docs/chinese-electricity-data-guide.md` lists 5 Chinese data platforms with URLs and step-by-step instructions for 和鲸
- [ ] All 5 notebooks have 学习目标 (Learning Objectives) displayed after the title
- [ ] All 5 notebooks have 思考题 (Reflection Questions) section with 2-4 questions at the end
- [ ] All 5 notebooks have try/except error handling for data loading operations
- [ ] Notebooks 04 and 05 have `pio.renderers.default = 'notebook'` configured
- [ ] No existing notebook functionality is broken — notebooks still execute top-to-bottom
- [ ] Docker Compose file validates as syntactically correct YAML (when uncommented)
</success_criteria>

<output>
Create `.planning/phases/01-data-foundation-basic-prediction/01-03-SUMMARY.md` when done
</output>
