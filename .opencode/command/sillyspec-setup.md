---
description: Install recommended MCP tools or list installed state
argument-hint: "[--list]"
tools:
  read: true
  bash: true
---
Run `sillyspec setup $ARGUMENTS` to install or inspect MCP tools.
- `sillyspec setup` — install recommended tools
- `sillyspec setup --list` — show installed tool status
