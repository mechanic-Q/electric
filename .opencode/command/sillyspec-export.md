---
description: Export successful implementation as reusable template
argument-hint: "[--change <name>] [--name <template-name>]"
tools:
  read: true
  write: true
  bash: true
  glob: true
  question: true
---
Run `sillyspec run export $ARGUMENTS` to generate a reusable template from completed work.

Extracts patterns and structure from the current change into a reusable template.
