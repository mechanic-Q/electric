---
phase: 01-data-foundation-basic-prediction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - setup.sh
  - requirements.txt
  - README.md
  - pipeline/__init__.py
  - pipeline/data_loader.py
  - pipeline/cleaner.py
  - pipeline/forecaster.py
  - notebooks/01_data_ingestion.ipynb
  - notebooks/05_end_to_end_baseline.ipynb
  - data/.gitkeep
  - .planning/phases/01-data-foundation-basic-prediction/SKELETON.md
autonomous: true
requirements: [ENV-01, ENV-03, DATA-01, DATA-02, DATA-03, PRED-01, VIZ-01]

must_haves:
  truths:
    - "Learner runs source setup.sh and all packages (pandas, scikit-learn, xgboost, plotly, jupyter) are importable"
    - "Learner imports OWIDChinaLoader and receives a DataFrame with Chinese electricity data (columns: timestamp, load_mw)"
    - "Learner runs Notebook 05 cells and sees a cumulative P&L chart rendered by plotly"
    - "DataLoader interface accepts multiple sources (OWID auto-fetch, Chinese manual files) without downstream code changes"
    - "Missing values are filled, IQR outliers are reported but NOT removed, timestamps are timezone-aware UTC"
  artifacts:
    - path: "setup.sh"
      provides: "One-command virtual environment creation with all pinned dependencies"
      contains: "python3.11 -m venv .venv"
    - path: "pipeline/data_loader.py"
      provides: "DataLoader ABC + OWIDChinaLoader + ChineseDataLoader implementations"
      exports: ["DataLoader", "OWIDChinaLoader", "ChineseDataLoader", "create_loader"]
      min_lines: 80
    - path: "pipeline/cleaner.py"
      provides: "Missing value fill, IQR outlier report-only, UTC timezone normalization"
      exports: ["clean_data", "validate_schema"]
    - path: "pipeline/forecaster.py"
      provides: "Persistence forecast (shift-24h) + minimal P&L calculation + plotly P&L chart"
      exports: ["persistence_forecast", "calculate_pnl", "plot_pnl"]
    - path: "notebooks/05_end_to_end_baseline.ipynb"
      provides: "End-to-end pipeline: load → clean → forecast → P&L → plotly chart"
  key_links:
    - from: "pipeline/data_loader.py OWIDChinaLoader.load_yearly_data()"
      to: "https://raw.githubusercontent.com/owid/energy-data/master/owid-energy-data.csv"
      via: "urllib.request + csv.DictReader, filter iso_code==CHN"
      pattern: "urllib\\.request\\.urlopen"
    - from: "pipeline/cleaner.py clean_data()"
      to: "DataLoader output (DataFrame with timestamp, load_mw)"
      via: "returns cleaned DataFrame; IQR detection reports via logging.warning, does not remove rows"
      pattern: "\\.ffill\\(\\)"
    - from: "pipeline/forecaster.py persistence_forecast()"
      to: "load_mw column"
      via: "pandas shift(24), backfill leading NaN"
      pattern: "\\.shift\\(24\\)"
    - from: "pipeline/forecaster.py calculate_pnl()"
      to: "flat price assumption ($50/MWh)"
      via: "(forecast - actual) * price_per_mwh, cumulative via cumsum()"
      pattern: "\\.cumsum\\(\\)"
    - from: "notebooks/05_end_to_end_baseline.ipynb"
      to: "pipeline.forecaster.plot_pnl()"
      via: "renders two-subplot plotly figure (actual vs forecast + cumulative P&L)"
      pattern: "plot_pnl"
---

## Phase Goal

**As a** learner, **I want to** install all dependencies with one command, load Chinese energy data, run a persistence forecast, and see a cumulative P&L chart, **so that** I prove the full data-to-trade pipeline works end-to-end before adding sophisticated models.

<objective>
Deliver the Walking Skeleton: the thinnest possible vertical slice through the entire Phase 1 pipeline — project scaffold → data loading (OWID China) → data cleaning → persistence forecast → P&L chart. After this plan, a learner can run `setup.sh`, open a single Jupyter notebook, execute all cells, and see an end-to-end result. This proves the architecture works before Plan 01-02 adds XGBoost and feature engineering.

Purpose: Establish the project skeleton (directory layout, package pins, module structure) and prove the full pipeline connects — which is Pitfall #5 prevention ("No end-to-end early" from PITFALLS.md). A working naive end-to-end run validates data format, module contracts, and visualization rendering in one shot.

Output: 10 production files (4 pipeline modules, 2 Jupyter notebooks, 4 config/infra files) + SKELETON.md architectural contract.
</objective>

<execution_context>
@/mnt/e/Ellectric/.opencode/get-shit-done/workflows/execute-plan.md
@/mnt/e/Ellectric/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@.planning/phases/01-data-foundation-basic-prediction/01-CONTEXT.md
@.planning/phases/01-data-foundation-basic-prediction/01-RESEARCH.md

## Key Decisions (from CONTEXT.md)
- **D-01**: Chinese electricity data only — OWID auto-fetch + manual download from 和鲸/天池/地方平台
- **D-04**: Parquet primary format
- **D-05**: Standardized schema: `timestamp` (datetime64[ns, UTC]), `load_mw` (float64)
- **D-06**: pip+venv, requirements.txt with pinned versions, <30min setup
- **D-10**: Modular .py modules (`pipeline/`) + thin Jupyter notebooks (`notebooks/`)
- **D-11**: Notebook naming: `01_data_ingestion.ipynb` through `05_end_to_end_baseline.ipynb`
- **D-12**: Persistence forecast baseline (yesterday's load = today's prediction)
- **D-13**: Minimal P&L chart (flat price $50/MWh, buy at forecast, settle at actual)
- **D-14**: MAE as primary metric (simpler than RMSE for learning)
- **D-15**: plotly for interactive visualizations

## Data Strategy (from RESEARCH.md + MVP context)
- **OWID yearly data**: auto-fetch from `https://raw.githubusercontent.com/owid/energy-data/master/owid-energy-data.csv`, filter `iso_code='CHN'`, extract columns: `year`, `electricity_generation`, `electricity_demand`, `coal_electricity`, `solar_electricity`, `wind_electricity`
- **ChineseDataLoader**: reads CSV/Parquet from `data/` directory (user manually downloads files)
- **NO international data**: no PUDL, no epftoolbox, no PJM

## Package List (pinned, per RESEARCH.md)
```
pandas==3.0.3
numpy>=2.0.0
pyarrow==22.0.0
scikit-learn==1.8.0
xgboost==3.2.0
plotly==6.7.0
jupyter==1.1.1
nbformat>=5.0.0
```
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Project scaffold — setup.sh, requirements.txt, directories, SKELETON.md, README.md</name>
  <files>setup.sh, requirements.txt, pipeline/__init__.py, notebooks/.gitkeep, data/.gitkeep, README.md, .planning/phases/01-data-foundation-basic-prediction/SKELETON.md</files>
  <read_first>
    @.planning/research/STACK.md (for pinned versions)
    @.planning/phases/01-data-foundation-basic-prediction/01-RESEARCH.md §2 (Environment Setup)
    @.planning/phases/01-data-foundation-basic-prediction/01-CONTEXT.md §decisions (D-06 env setup, D-10 modular structure)
  </read_first>
  <action>
    Create project scaffold per D-06 and D-10:

    1. Create `setup.sh` with shebang `#!/usr/bin/env bash`, `set -euo pipefail`. Commands:
       - `python3.11 -m venv .venv` (create virtual environment)
       - `source .venv/bin/activate` (or `Scripts/activate` on Windows, detect via `$OSTYPE`)
       - `pip install -r requirements.txt`
       - Print success message listing installed packages.
       - Add a comment block at top: `# If downloads are slow in China, uncomment the following line and add \`-i https://pypi.tuna.tsinghua.edu.cn/simple\` to the pip install command.`
       - Use `chmod +x setup.sh` after creation.

    2. Create `requirements.txt` with contents exactly:
       ```
       # Core data processing
       pandas==3.0.3
       numpy>=2.0.0
       pyarrow==22.0.0

       # Machine learning
       scikit-learn==1.8.0
       xgboost==3.2.0

       # Visualization
       plotly==6.7.0

       # Development
       jupyter==1.1.1
       nbformat>=5.0.0
       ```

    3. Create directory structure:
       - `mkdir -p pipeline notebooks data` (bash; real mkdir + .gitkeep files)
       - `pipeline/__init__.py`: empty file (makes pipeline a Python package)
       - `notebooks/.gitkeep`: empty file
       - `data/.gitkeep`: empty file

    4. Create `README.md` stub at project root with:
       - Title: `# Ellectric — AI + 电力交易技术学习平台`
       - Badge-style status: `**Phase 1:** Data Foundation + Basic Prediction`
       - One-line description: "A hands-on learning platform for AI-driven electricity trading — end-to-end pipeline from public data to automated trading strategies."
       - Quick start section: references `source setup.sh` and `jupyter notebook`.

    5. Verify that SKELETON.md already exists at `.planning/phases/01-data-foundation-basic-prediction/SKELETON.md`. DO NOT overwrite it if present. If missing, note to executor: SKELETON.md was created by planner; validate its existence.
  </action>
  <verify>
    <automated>bash -c 'test -f setup.sh && test -f requirements.txt && test -f pipeline/__init__.py && test -f data/.gitkeep && test -f notebooks/.gitkeep && test -f README.md && echo "ALL FILES EXIST"'</automated>
    <automated>bash setup.sh 2>&1 | head -20</automated>
    <automated>source .venv/bin/activate && python -c "import pandas; import sklearn; import xgboost; import plotly; import pyarrow; print('ALL IMPORTS OK')"</automated>
  </verify>
  <done>
    - `setup.sh` creates venv and installs all dependencies within 30 minutes on a clean Python 3.11 machine
    - `requirements.txt` has all 8 packages with pinned versions
    - Project directory structure (`pipeline/`, `notebooks/`, `data/`) exists with `.gitkeep` files
    - `README.md` has project title, phase status, and quick start instructions
    - All packages importable without errors
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: DataLoader interface + OWIDChinaLoader + ChineseDataLoader (pipeline/data_loader.py)</name>
  <files>pipeline/data_loader.py</files>
  <read_first>
    @.planning/phases/01-data-foundation-basic-prediction/01-CONTEXT.md §decisions (D-01, D-03, D-04, D-05)
    @.planning/phases/01-data-foundation-basic-prediction/01-RESEARCH.md §1 (OWID data source, URL, columns) and §5 (DataLoader interface pattern)
  </read_first>
  <action>
    Create `pipeline/data_loader.py` per D-01 (China data), D-04 (Parquet), D-05 (standardized schema), and RESEARCH.md §1 (OWID strategy). Implement three classes:

    **DataLoader ABC** (abstract base class):
    - `load_data(start=None, end=None) -> pd.DataFrame`: returns DataFrame with columns [timestamp, load_mw] + optional [region, data_source]. All timestamps MUST be datetime64[ns, UTC] with timezone-aware dtype.
    - `data_version` property (string) for data lineage tracking.
    - `get_metadata() -> dict` returning `{source, region, frequency, timerange, missing_pct}`.

    **OWIDChinaLoader(DataLoader)**:
    - `__init__(self, cache_dir='data/')`: sets cache path for downloaded raw CSV.
    - `load_data(start=None, end=None)`:
      a. Fetch `https://raw.githubusercontent.com/owid/energy-data/master/owid-energy-data.csv` via `urllib.request.urlopen`. Set User-Agent header to "ellectric/1.0".
      b. Cache raw response to `{cache_dir}/owid_energy_raw.csv` (write bytes). On subsequent calls, read from cache if file exists (offline resilience).
      c. Parse CSV with `csv.DictReader` — filter rows where `iso_code == 'CHN'`.
      d. Extract columns: `year`, `electricity_generation`, `electricity_demand`, `coal_electricity`, `solar_electricity`, `wind_electricity`.
      e. Map columns: `year` → `timestamp` (via `pd.to_datetime(df['year'], format='%Y')` then `timestamp.dt.tz_localize('UTC')`); `electricity_demand` → `load_mw` (cast to float64, NaN→NaN OK).
      f. Add metadata columns: `data_source='OWID'`, `region='China'`.
      g. Filter by start/end if provided.
      h. Set `data_version` to current date string (e.g., "owid-2026-05-20") since commit hash fetch adds complexity — use date of fetch.
    - Error handling: if URL fetch fails, print clear error message with URL and suggest checking network. If cached file exists, load from cache with warning. If no cache and no network, raise RuntimeError.

    **ChineseDataLoader(DataLoader)**:
    - `__init__(self, data_path: str)`: `data_path` is path to CSV or Parquet file in `data/` directory.
    - `load_data(start=None, end=None)`:
      a. Detect format by extension: `.csv` → `pd.read_csv()`, `.parquet` → `pd.read_parquet()`.
      b. Validate that required columns `[timestamp, load_mw]` exist in loaded DataFrame. If missing, raise ValueError with available column names.
      c. Cast `timestamp` to datetime64[ns, UTC]: if timezone-naive, `dt.tz_localize('UTC')`; if already timezone-aware, `dt.tz_convert('UTC')`.
      d. Cast `load_mw` to float64. Drop NaN rows in load_mw (these are genuinely missing load values).
      e. Add/overwrite metadata columns: `data_source='chinese_platform'`, region from optional column or default `'China'`.
      f. Filter by start/end if provided.
    - If `data_path` file does not exist: print a WARNING log with instructions for manual download (reference the upcoming docs/chinese-electricity-data-guide.md), return empty DataFrame with correct schema columns.
    - Set `data_version` to file modification time (mtime) via `os.path.getmtime`.

    **Factory function**:
    - `create_loader(source: str, **kwargs) -> DataLoader`: if source == 'owid', returns OWIDChinaLoader(). if source == 'chinese', returns ChineseDataLoader(data_path=kwargs['data_path']). else raises ValueError.

    **Imports used at top of file**: `import pandas as pd`, `import urllib.request`, `import csv`, `import os`, `import logging`, `from abc import ABC, abstractmethod`, `from io import StringIO`.
  </action>
  <verify>
    <automated>cd /mnt/e/Ellectric && source .venv/bin/activate && python -c "
from pipeline.data_loader import OWIDChinaLoader, ChineseDataLoader, create_loader
# Test OWIDChinaLoader
loader = OWIDChinaLoader()
df = loader.load_data()
assert 'timestamp' in df.columns, 'Missing timestamp column'
assert 'load_mw' in df.columns, 'Missing load_mw column'
assert str(df['timestamp'].dtype) == 'datetime64[ns, UTC]', f'Wrong dtype: {df[\"timestamp\"].dtype}'
assert df['load_mw'].dtype == 'float64', f'Wrong dtype: {df[\"load_mw\"].dtype}'
assert len(df) > 0, 'Empty DataFrame'
print(f'OWIDChinaLoader: {len(df)} rows loaded')
print(df.head())
"</automated>
  </verify>
  <done>
    - `OWIDChinaLoader().load_data()` returns DataFrame with columns [timestamp (datetime64[ns, UTC]), load_mw (float64), data_source, region]
    - Data is cached to `data/owid_energy_raw.csv` for offline resilience
    - `ChineseDataLoader(data_path).load_data()` validates columns and standardizes timestamps
    - `create_loader('owid')` and `create_loader('chinese', data_path='...')` return correct loader types
    - Missing file in ChineseDataLoader returns empty DataFrame with warning, not exception
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Data cleaning module + persistence forecaster + P&L + Notebooks 01 + 05</name>
  <files>pipeline/cleaner.py, pipeline/forecaster.py, notebooks/01_data_ingestion.ipynb, notebooks/05_end_to_end_baseline.ipynb</files>
  <read_first>
    @.planning/phases/01-data-foundation-basic-prediction/01-CONTEXT.md §decisions (D-03 manual download, D-05 UTC schema, D-12 persistence, D-13 P&L chart, D-15 plotly)
    @.planning/phases/01-data-foundation-basic-prediction/01-RESEARCH.md §3 (persistence + P&L implementations) and §4 (plotly patterns)
    @.planning/research/PITFALLS.md §Pitfall 2 (IQR is REPORT-ONLY, spikes are signal, never remove outliers) and §Pitfall 5 (end-to-end early)
  </read_first>
  <action>
    Create two pipeline modules and two Jupyter notebooks per D-10 (thin notebooks, thick modules), D-11 (naming convention), D-12 (persistence baseline), D-13 (P&L chart), D-15 (plotly).

    ---

    **pipeline/cleaner.py** — implements cleaning pipeline per DATA-03, with Pitfall 2 enforcement (outliers reported, NOT removed):

    Function `clean_data(df: pd.DataFrame) -> pd.DataFrame`:
    - Step 1: `handle_timezone(df)`: if timestamp is timezone-naive, `dt.tz_localize('UTC')`; if timezone-aware (different from UTC), `dt.tz_convert('UTC')`. Ensures all timestamps are UTC-aware datetime64[ns, UTC].
    - Step 2: `remove_duplicates(df)`: drop duplicate timestamps, keeping first occurrence. Log count of duplicates removed via `logging.info`.
    - Step 3: `fill_missing(df)`: fill missing `load_mw` values using forward-fill (`ffill()`) then backward-fill (`bfill()`). Log count of missing values before/after. If ALL values are NaN (edge case), raise ValueError.
    - Step 4: `detect_outliers(df)`: compute IQR (Q1-1.5*IQR, Q3+1.5*IQR). Log the count and indices of outlier timestamps via `logging.warning`. CRITICAL: do NOT remove or modify these rows — IQR detection is REPORT-ONLY per PITFALLS.md Pitfall 2 (price/load spikes are the signal). Return the DataFrame unchanged after reporting.
    - Step 5: Sort by timestamp ascending, reset index.

    Function `validate_schema(df: pd.DataFrame) -> bool`:
    - Check required columns `['timestamp', 'load_mw']` exist. If missing, log which are missing and return False.
    - Check timestamp dtype is `datetime64[ns, UTC]`. If not, log the actual dtype and return False.
    - Check load_mw dtype is float-compatible. Return True if all valid, False otherwise.

    Function `get_cleaning_report(df_before: pd.DataFrame, df_after: pd.DataFrame) -> str`:
    - Returns a formatted string showing: row count change, missing values filled, duplicates removed, outlier count (from reporting step), timezone status.

    ---

    **pipeline/forecaster.py** — implements persistence baseline + minimal P&L per D-12, D-13:

    Function `persistence_forecast(df: pd.DataFrame, shift_hours: int = 24) -> pd.Series`:
    - Shift `load_mw` column by `shift_hours` periods: `df['load_mw'].shift(shift_hours)`.
    - Fill leading NaN values (the first `shift_hours` rows) with `fillna(method='bfill')`.
    - Return predicted Series with same index as input.

    Function `calculate_pnl(actual: pd.Series, forecast: pd.Series, price_per_mwh: float = 50.0) -> pd.DataFrame`:
    - `trade_pnl = (forecast - actual) * price_per_mwh` — positive when over-forecast (bought less than needed at lower forecast price), negative when under-forecast.
    - `cumulative_pnl = trade_pnl.cumsum()`.
    - Return DataFrame with columns: `[timestamp, actual_mw, forecast_mw, trade_pnl, cumulative_pnl]`. Use actual Series index for timestamp if available, else range index.

    Function `plot_pnl(pnl_df: pd.DataFrame) -> go.Figure`:
    - Use `plotly.subplots.make_subplots(rows=2, cols=1, shared_xaxes=True, vertical_spacing=0.08, subplot_titles=('Actual vs Persistence Forecast', 'Cumulative P&L'))`.
    - Subplot 1 (top): `go.Scatter` for actual load (`actual_mw`, line=blue, name='Actual') and predicted load (`forecast_mw`, line=orange, name='Persistence Forecast').
    - Subplot 2 (bottom): `go.Scatter` for `cumulative_pnl` (line=green if positive, red if negative; use fill='tozeroy' for area chart).
    - Add `fig.add_hline(y=0, line_dash='dash', line_color='gray')` to subplot 2.
    - Layout: title='End-to-End Baseline: Persistence Forecast → P&L', xaxis2_title='Time', yaxis_title='Load (MW)', yaxis2_title='Cumulative P&L ($)'.
    - Set `pio.renderers.default = 'notebook'` for Jupyter rendering.

    ---

    **notebooks/01_data_ingestion.ipynb** — demonstrates data loading per D-03, D-10:

    Structure (each section = one markdown cell + one code cell):
    1. Markdown: `# 01 — 数据获取 (Data Ingestion)` with learning objectives: understand OWID China data, practice using DataLoader interface, explore data dimensions.
    2. Code: `from pipeline.data_loader import OWIDChinaLoader, ChineseDataLoader, create_loader`
    3. Markdown: `### 1.1 OWID 中国年度电力数据 (自动获取)`
    4. Code: `loader = OWIDChinaLoader(); df_yearly = loader.load_data(); print(f"Loaded {len(df_yearly)} rows, {df_yearly['year'].min()}-{df_yearly['year'].max()}"); df_yearly.head(10)`
    5. Code: `df_yearly.describe()` (basic stats)
    6. Code: `df_yearly[['year', 'electricity_generation', 'electricity_demand']].set_index('year').plot(title='China Electricity Generation vs Demand (OWID)')` using pandas `.plot()` for quick matplotlib view.
    7. Markdown: `### 1.2 中国本地数据 (手动下载)` with note: "Chinese platforms (和鲸/天池/地方平台) require manual browser download. Place files in data/ directory. See docs/chinese-electricity-data-guide.md."
    8. Code: `loader_cn = ChineseDataLoader(data_path='data/example_load.csv')` — wrapped in try/except FileNotFoundError with clear message if no file exists.
    9. Markdown: `### 思考题 (Reflection)` with 3 questions: (a) What years show fastest electricity growth? (b) What is the difference between generation and demand? (c) What data frequency would you need for hourly load forecasting?

    ---

    **notebooks/05_end_to_end_baseline.ipynb** — proves full pipeline connects per D-12, D-13, D-15, Pitfall 5:

    Structure:
    1. Markdown: `# 05 — 端到端基线：持久性预测 → P&L (End-to-End Baseline)` with explanation: "This notebook proves all pipeline layers connect. We use a naive persistence forecast (yesterday's load = today's prediction) and a flat price assumption to calculate hypothetical trading profit/loss."
    2. Code: Imports — `from pipeline.data_loader import OWIDChinaLoader; from pipeline.cleaner import clean_data, get_cleaning_report; from pipeline.forecaster import persistence_forecast, calculate_pnl, plot_pnl; import plotly.io as pio; pio.renderers.default = 'notebook'`
    3. Markdown: `### Step 1: Load Data`
    4. Code: `loader = OWIDChinaLoader(); df_raw = loader.load_data(); print(f"Raw data: {len(df_raw)} rows"); df_raw.head()`
    5. Code: `df_raw.info()` and `df_raw.describe()`
    6. Markdown: `### Step 2: Clean Data`
    7. Code: `df_clean = clean_data(df_raw); report = get_cleaning_report(df_raw, df_clean); print(report); df_clean.head()`
    8. Markdown: `### Step 3: Persistence Forecast`
    9. Code: `df_clean['forecast_mw'] = persistence_forecast(df_clean, shift_hours=24); df_clean[['timestamp', 'load_mw', 'forecast_mw']].head(30)` (NB: for yearly OWID data, shift=1 period, not 24h. Adapt: use `shift_hours=1` for yearly data, with comment explaining hourly data would use 24.)
    10. Markdown: `### Step 4: Calculate P&L`
    11. Code: `pnl_df = calculate_pnl(df_clean['load_mw'], df_clean['forecast_mw'], price_per_mwh=50.0); pnl_df.head(10)`
    12. Markdown: `### Step 5: Visualize Results`
    13. Code: `fig = plot_pnl(pnl_df); fig.show()`
    14. Markdown: `### 思考题 (Reflection)` with questions: (a) Why is the persistence forecast pattern shifted by exactly one data point? (b) What does a positive cumulative P&L mean for a trader? (c) What limitations does yearly data have for real trading decisions? (d) How would hourly data change the persistence forecast approach?
  </action>
  <verify>
    <automated>cd /mnt/e/Ellectric && source .venv/bin/activate && python -c "
from pipeline.data_loader import OWIDChinaLoader
from pipeline.cleaner import clean_data, validate_schema, get_cleaning_report
from pipeline.forecaster import persistence_forecast, calculate_pnl, plot_pnl

# Load data
loader = OWIDChinaLoader()
df = loader.load_data()

# Clean
df_before = df.copy()
df_clean = clean_data(df)
report = get_cleaning_report(df_before, df_clean)
print('Cleaning report:', report[:200])
assert validate_schema(df_clean), 'Schema validation failed'

# Persistence forecast
forecast = persistence_forecast(df_clean, shift_hours=1)
assert len(forecast) == len(df_clean), 'Forecast length mismatch'
assert not forecast.isna().any(), 'Forecast contains NaN'

# P&L
pnl = calculate_pnl(df_clean['load_mw'], forecast, price_per_mwh=50.0)
assert 'cumulative_pnl' in pnl.columns, 'Missing cumulative_pnl'
assert len(pnl) == len(df_clean), 'P&L length mismatch'

# Plot (verify it returns a figure)
fig = plot_pnl(pnl)
assert hasattr(fig, 'show'), 'Not a plotly figure'

print('All end-to-end checks passed')
"</automated>
  </verify>
  <done>
    - `clean_data()` fills missing values with forward/backward fill, normalizes timestamps to UTC, reports IQR outliers via logging (does NOT remove them)
    - `validate_schema()` returns True for valid DataFrames, False with logged reasons for invalid ones
    - `persistence_forecast()` with shift_hours=1 on yearly OWID data produces forecast with no NaN values
    - `calculate_pnl()` returns DataFrame with cumulative_pnl column; cumulative sum tracks over time
    - `plot_pnl()` returns a plotly Figure with two subplots (actual vs forecast + cumulative P&L)
    - Notebook 01 imports from pipeline modules, loads OWID data, displays head() and describe(), handles missing ChineseDataLoader file gracefully
    - Notebook 05 executes all pipeline steps sequentially and renders an interactive plotly chart
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
There are no trust boundaries in Phase 1 — this is a local-only Jupyter learning environment with no network services, no API endpoints, no user authentication, and no database server. All code runs locally on the learner's machine.

| Boundary | Description |
|----------|-------------|
| PyPI → local venv | Python packages installed from PyPI cross this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | `pip install` from PyPI | accept | PyPI is the trusted Python package repository. Pinned versions in requirements.txt prevent unexpected upgrades. No supply-chain hardening (e.g., `--require-hashes`) for Phase 1 — accepted for a learning platform with no production data. |
| T-01-02 | Spoofing | OWID CSV fetched via urllib | accept | Data fetched over HTTPS from GitHub raw (`raw.githubusercontent.com`). GitHub TLS provides transport integrity. OWID is a reputable academic source (Oxford). No PII or financial data involved — accepted for learning purposes. |
| T-01-03 | Tampering | ChineseDataLoader reads local CSV/Parquet | accept | User manually downloads these files — they are not auto-fetched. The user controls what goes in `data/`. No external network dependency for this path. |
| T-01-04 | Information Disclosure | None | accept | No user data, no API keys, no secrets in this phase. All data is public. |
| T-01-05 | Denial of Service | None | accept | Local-only Jupyter server. No external attack surface. |
| T-01-06 | Elevation of Privilege | None | accept | No authentication/authorization system exists. Local Jupyter runs as the user's OS user. |
</threat_model>

<verification>
Run all automated verification commands after Task 3 completes:
```bash
cd /mnt/e/Ellectric && source .venv/bin/activate && python -c "
from pipeline.data_loader import OWIDChinaLoader, ChineseDataLoader, create_loader
from pipeline.cleaner import clean_data, validate_schema, get_cleaning_report
from pipeline.forecaster import persistence_forecast, calculate_pnl, plot_pnl

# Full pipeline test
loader = OWIDChinaLoader()
df = loader.load_data()
df_clean = clean_data(df)
forecast = persistence_forecast(df_clean, shift_hours=1)
pnl = calculate_pnl(df_clean['load_mw'], forecast)
fig = plot_pnl(pnl)
print('Walking Skeleton verified: data→clean→forecast→P&L→plot')
print(f'Rows: {len(df)}, Clean rows: {len(df_clean)}')
print(f'Cumulative P&L final: \${pnl[\"cumulative_pnl\"].iloc[-1]:.2f}')
"
```

Check that:
- setup.sh exists, is executable, and the venv is creatable
- All 8 packages are importable
- OWIDChinaLoader successfully fetches and parses Chinese data
- clean_data handles timezone, missing values, duplicates
- IQR outlier detection reports but does NOT remove rows
- persistence_forecast returns valid non-NaN series
- calculate_pnl returns cumulative_pnl column
- plot_pnl returns a renderable plotly figure
</verification>

<success_criteria>
- [ ] `setup.sh` creates venv with all 8 packages importable (<30 min on clean Python 3.11 machine)
- [ ] OWIDChinaLoader auto-fetches Chinese energy data from OWID and returns standardized DataFrame
- [ ] ChineseDataLoader exists as skeleton with clear error message for missing files
- [ ] `clean_data()` fills missing values, normalizes timezone to UTC, reports (not removes) IQR outliers
- [ ] `persistence_forecast()` outputs non-NaN predictions
- [ ] `calculate_pnl()` computes cumulative P&L from forecast vs actual
- [ ] `plot_pnl()` returns an interactive plotly Figure with two linked subplots
- [ ] Notebook 01 imports pipeline modules, loads OWID data, and handles missing Chinese files gracefully
- [ ] Notebook 05 runs end-to-end (load → clean → forecast → P&L → chart) in sequence without errors
- [ ] All automated verification commands pass
</success_criteria>

<output>
Create `.planning/phases/01-data-foundation-basic-prediction/01-01-SUMMARY.md` when done
</output>
